# 上游同步分析 — 第七批（2026-07-08）

> 本文档为第七批上游同步检查的完整分析记录，包含逐提交 diff 分析、本地代码核对、适配要点。
> 进度摘要见 [`docs/dev/upstream-sync.md`](../upstream-sync.md)。
>
> **同步状态：✅ 全部完成（编译通过 + 单元测试通过 + 逐文件核对一致）**

---

## 一、背景

### 1.1 项目概况

- **项目**：Measix Pilot — 从 [RikkaHub](https://github.com/rikkahub/rikkahub) fork 的 Android LLM 聊天客户端
- **Fork 基线**：RikkaHub 2.3.1（versionCode 164）— 2026-06-18
- **本地当前版本**：0.0.9（versionCode 9）
- **本地 DB 版本**：version = 3（含文件夹表）
- **包名映射**：上游 `me.rerere.rikkahub` → 本地 `net.weero.measix.pilot`
- **DeepLink scheme**：上游 `rikkahub://` → 本地 `measix://`

### 1.2 上次同步状态

- **上次检查点**：`624ab635`（2026-07-08，第六批）
- **上次同步范围**：`ef564dca..624ab635`（2026-07-06 ~ 2026-07-08，21 个提交，16 个已同步 + 1 个补充同步）

### 1.3 本次检查范围

- **检查范围**：`624ab635..upstream/master`（2026-07-08，共 **10 个提交**）
- **最新上游提交**：`8e872e5f`（2026-07-08，聊天输入框键盘收起间距）
- **上游版本**：2.4.1（versionCode 169，未升级）

### 1.4 版本号对应关系

| 维度 | 上游 | 本地 | 说明 |
|------|------|------|------|
| 版本名 | 2.4.1 | 0.0.9 | 版本线独立，不跟随上游版本号 |
| versionCode | 169 | 9 | 独立递增 |
| DB 版本 | v24 | v3 | 本地从 v1 重建，无历史 migration 包袱 |
| MCP SDK | 0.14.0 | 0.14.0 | batch-4 已对齐 |

> 本批同步不递增本地版本号（`versionCode`/`versionName` 保持 9/0.0.9），改动以 bug 修复和功能增强为主。

---

## 二、提交逐项分析

### 2.1 `6a35f89e` — feat(tts): 支持 provider 向 AI 注入语气标记引导并接入 MiMo

| 项目 | 内容 |
|------|------|
| 描述 | TTSProvider 新增可选 `promptGuidance` 属性，TTSManager 新增 `getPromptGuidance()`，text_to_speech 工具启用时将当前选中 provider 的引导注入 system prompt，为 MiMo 硬编码风格/音频标签引导 |
| 影响文件 | 7 文件 |
| 日期 | 2026-07-08 |

**上游逐文件 diff 与本地核对**：

#### 文件 1：`speech/.../TTSProvider.kt` — 接口新增 promptGuidance

**上游 diff**：接口新增可选属性
```kotlin
val promptGuidance: String
    get() = ""
```

**本地核对**：`TTSProvider.kt` 无 `promptGuidance` ❌

#### 文件 2：`speech/.../TTSManager.kt` — 新增 getPromptGuidance 方法

**上游 diff**：新增方法遍历所有 provider（11 个分支）返回 `promptGuidance`

**本地核对**：本地 `TTSManager` 仅 4 个 provider（OpenAI/Gemini/SystemTTS/MiMo），无 `getPromptGuidance` ❌

**适配**：本地 when 分支仅需 4 个（OpenAI/Gemini/SystemTTS/MiMo）

#### 文件 3：`speech/.../TTSProviderSetting.kt` — MiMo 默认模型更新

**上游 diff**：`model: String = "mimo-v2-tts"` → `"mimo-v2.5-tts"`

**本地核对**：本地已是 `"mimo-v2.5-tts"` ✅（batch-1 引入时即为 v2.5）

#### 文件 4：`speech/.../MiMoTTSProvider.kt` — 硬编码 promptGuidance

**上游 diff**：新增 `override val promptGuidance` 属性（~22 行），包含风格标签 + 内联音频标签说明

**本地核对**：本地无 `promptGuidance` ❌

#### 文件 5：`app/.../tools/local/LocalTools.kt` — 构造函数新增依赖

**上游 diff**：
```kotlin
// 旧：
class LocalTools(private val context: Context, private val eventBus: AppEventBus)
// 新：
class LocalTools(
    private val context: Context,
    private val eventBus: AppEventBus,
    private val ttsManager: TTSManager,
    private val settingsStore: SettingsStore,
)
```
`ttsTool` 改为 `buildTextToSpeechTool(eventBus, ttsManager, settingsStore)`

**本地核对**：`LocalTools.kt:7` — `class LocalTools(private val context: Context, private val eventBus: AppEventBus)` ❌

#### 文件 6：`app/.../tools/local/TextToSpeechTool.kt` — 新增 systemPrompt 注入

**上游 diff**：
```kotlin
// 旧：
internal fun buildTextToSpeechTool(eventBus: AppEventBus): Tool = Tool(
// 新：
internal fun buildTextToSpeechTool(
    eventBus: AppEventBus,
    ttsManager: TTSManager,
    settingsStore: SettingsStore,
): Tool = Tool(
    ...
    systemPrompt = { _, _ ->
        settingsStore.settingsFlow.value.getSelectedTTSProvider()
            ?.let { ttsManager.getPromptGuidance(it) }
            .orEmpty()
    },
```

**本地核对**：`TextToSpeechTool.kt:14` — `internal fun buildTextToSpeechTool(eventBus: AppEventBus)` ❌

#### 文件 7：`app/.../di/AppModule.kt` — LocalTools 注册参数更新

**上游 diff**：`LocalTools(get(), get())` → `LocalTools(get(), get(), get(), get())`

**本地核对**：`AppModule.kt:29` — `LocalTools(get(), get())` ❌

**判定**：✅ **同步** — 功能增强，本地有 MiMo TTS（batch-1 引入），语气标记引导对 TTS 体验有实质提升

**适配要点**：
- 本地 `TTSManager` 仅 4 个 provider 分支（非上游 11 个）
- `getSelectedTTSProvider()` 已在 `PreferencesStore.kt:598` 定义 ✅
- `settingsStore.settingsFlow` 已在本地存在 ✅

---

### 2.2 `45191d27` — feat(tts): MiniMax 移除 emotion 参数改为自动

| 项目 | 内容 |
|------|------|
| 描述 | MiniMax TTS 移除 emotion 参数字段，改为自动检测 |
| 影响文件 | 3 文件 |
| 日期 | 2026-07-08 |

**本地核对**：本地无 MiniMax TTS provider（fork 精简时移除）

**判定**：❌ **跳过** — 本地无 MiniMax TTS

---

### 2.3 `c9394d94` — CI: 每日构建替换手动 Release 构建

**判定**：❌ **跳过** — CI 配置，不适用

---

### 2.4 `6ce74f71` — CI: 每日构建拉取 git submodule

**判定**：❌ **跳过** — CI 配置，不适用

---

### 2.5 `feeed5dc` — CI: 每日构建 Prerelease

**判定**：❌ **跳过** — CI 配置，不适用

---

### 2.6 `baf72a21` — chore: bump to 2.4.1

**判定**：❌ **跳过** — 版本线独立

---

### 2.7 `8477334d` — fix(mcp): SSE 通知流重试耗尽不再触发整体重连

| 项目 | 内容 |
|------|------|
| 描述 | StreamableHttpClientTransport 额外开一条 GET SSE 长连接，部分 server 不支持或主动关闭。SDK 内部重试耗尽后 emit "Maximum reconnection attempts exceeded"，此时 POST 通道仍健康，旧逻辑据此重建整个客户端，形成无限重连循环。新增 `isSseStreamGiveUpError` 过滤该错误 |
| 影响文件 | `McpManager.kt` |
| 日期 | 2026-07-08 |

**上游 diff（2 处 onError + 1 个新方法）**：

1. 两处 `transport.onError` 中新增 `if (isSseStreamGiveUpError(error)) return@onError`
2. 新增 `isSseStreamGiveUpError` 方法：
```kotlin
private fun isSseStreamGiveUpError(error: Throwable): Boolean {
    val message = generateSequence(error) { it.cause }
        .mapNotNull { it.message }
        .joinToString(" ")
    return message.contains("Maximum reconnection attempts exceeded", ignoreCase = true)
}
```

**本地核对**：
- 本地 `McpManager.kt:524` — `transport.onError` **无** `isSseStreamGiveUpError` 过滤 ❌
- 本地仅有 1 处 `transport.onError`（上游有 2 处，本地 batch-3 MCP 重构后结构不同）
- 本地 `looksUnauthorized` 方法已存在（batch-4 OAuth），`isSseStreamGiveUpError` 可在其旁边添加

**判定**：✅ **同步** — P0 bug 修复，MCP 无限重连循环

**适配要点**：本地仅有 1 处 `transport.onError`（`:524`），在该处添加过滤 + 新增 `isSseStreamGiveUpError` 方法（放在 `looksUnauthorized` 旁）

---

### 2.8 `7d88bbc3` — fix(ai): 修复流式输出丢字并将生成通知拆分到独立组件 (#1295)

| 项目 | 内容 |
|------|------|
| 描述 | 4 个独立改动：① Provider callbackFlow 无界缓冲修复丢字；② ClaudeProvider trySend 移到 close() 前；③ replaceRegexes SimpleCache 缓存；④ ChatNotificationManager 通知拆分 |
| 影响文件 | 11 文件 |
| 日期 | 2026-07-08 |

**上游逐文件 diff 与本地核对**：

#### 改动 A：Provider callbackFlow 无界缓冲（4 文件）— P0

##### A1. `ClaudeProvider.kt`

**上游 diff**：
- 新增 import：`Channel`, `onFailure`, `buffer`
- `trySend(messageChunk)` 从 `when` 块后移到 `when` 块前，改为 `trySend(messageChunk).onFailure { e -> Log.w(...) }`
- `callbackFlow { ... }` 末尾改为 `}.buffer(Channel.UNLIMITED)`

**本地核对**：`ClaudeProvider.kt:226` — `trySend(messageChunk)` 在 `when` 块后（最后位置）❌
- 本地 `:262` — `}` 末尾无 `.buffer(Channel.UNLIMITED)` ❌

##### A2. `GoogleProvider.kt`

**上游 diff**：
- 新增 import：`Channel`, `onFailure`, `buffer`
- `trySend(messageChunk)` → `trySend(messageChunk).onFailure { e -> Log.w(...) }`
- `callbackFlow { ... }` 末尾改为 `}.buffer(Channel.UNLIMITED)`

**本地核对**：`GoogleProvider.kt:289` — `trySend(messageChunk)` 无 onFailure ❌
- 本地 `:343` — `}` 末尾无 `.buffer()` ❌

##### A3. `ChatCompletionsAPI.kt`

**上游 diff**：同 GoogleProvider 模式

**本地核对**：`ChatCompletionsAPI.kt:208` — `trySend(messageChunk)` 无 onFailure ❌
- 本地 `:246` — `}` 末尾无 `.buffer()` ❌

##### A4. `ResponseAPI.kt`

**上游 diff**：同模式

**本地核对**：`ResponseAPI.kt:145` — `trySend(chunk)` 无 onFailure ❌
- 本地 `:186` — `}` 末尾无 `.buffer()` ❌

#### 改动 B：replaceRegexes SimpleCache 缓存 — P1

**上游 diff**（`Assistant.kt`）：
- 新增 import：`SimpleCache`, `TimeUnit`
- 新增 `regexCache` + `compileRegexCached()` 函数
- `replaceRegexes` 中 `Regex(regex.findRegex)` 改为 `compileRegexCached(regex.findRegex) ?: return@fold acc`

**本地核对**：`Assistant.kt:80-105` — 无缓存，每次 `Regex(regex.findRegex)` 重新编译 ❌

#### 改动 C：ChatNotificationManager 通知拆分 — 同步

**上游 diff（6 文件）**：

##### C1. `AppEvent.kt` — 新增 2 个事件类型
- `ChatGenerationUpdate(conversationId, lastMessage, senderName)`
- `ChatGenerationEnded(conversationId, senderName, contentPreview?)`

**本地核对**：`AppEvent.kt` 无这两个类型 ❌

##### C2. `AppEventBus.kt` — 新增 tryEmit
```kotlin
fun tryEmit(event: AppEvent): Boolean = _events.tryEmit(event)
```

**本地核对**：`AppEventBus.kt` 无 `tryEmit` ❌

##### C3. `ChatNotificationManager.kt` — 新建文件（201 行）
- 承接 ChatService 的全部通知逻辑（Live Update + 完成通知）
- 新增 1s 节流（`LIVE_UPDATE_NOTIFICATION_THROTTLE_MS`）
- ProcessLifecycleOwner 前后台检测
- 订阅 AppEventBus 消费事件

**本地核对**：本地无此文件 ❌

##### C4. `ChatService.kt` — 通知逻辑移出
- 新增 `appEventBus: AppEventBus` 依赖
- 删除 `isForeground`/`lifecycleObserver`/`init{}` 块
- 删除 `cleanup()` 中的 `removeObserver`
- `.onCompletion` 中 `cancelLiveUpdateNotification` + `sendGenerationDoneNotification` → `appEventBus.emit(ChatGenerationEnded(...))`
- `.collect` 中 `sendLiveUpdateNotification` → `appEventBus.tryEmit(ChatGenerationUpdate(...))`
- `.onFailure` 中 `cancelLiveUpdateNotification` → `appEventBus.tryEmit(ChatGenerationEnded(..., null))`
- 删除全部通知私有方法（~118 行）

**本地核对**：
- 本地 ChatService 有 `isForeground`/`lifecycleObserver`/通知方法 ❌（需移出）
- ⚠️ 本地 ChatService 有声音反馈逻辑（batch-0.0.4 引入），上游无此逻辑，**保留**
- ⚠️ 本地 ChatService 有 `soundEffectPlayer` 依赖，上游无，**保留**

##### C5. `RouteActivity.kt` — 新增事件分支
```kotlin
is AppEvent.ChatGenerationUpdate -> Unit
is AppEvent.ChatGenerationEnded -> Unit
```
+ 格式修复（OkHttpNetworkFetcherFactory 缩进、`conversationId` 行尾换行、`popTransitionSpec` 缩进）

**本地核对**：`RouteActivity.kt:249` — when 分支无这两个事件 ❌

##### C6. `AppModule.kt` — 注册 ChatNotificationManager + ChatService 新增参数
```kotlin
single(createdAtStart = true) {
    ChatNotificationManager(get(), get(), get(), get())
}
// ChatService 新增 appEventBus = get()
```

**本地核对**：`AppModule.kt` 无 ChatNotificationManager 注册 ❌

**判定**：✅ **同步** — P0 bug 修复（丢字）+ P1 性能优化（缓存）+ 架构改进（通知解耦）

**适配要点**：
- 4 个 Provider：添加 `buffer(Channel.UNLIMITED)` + `trySend().onFailure`
- ClaudeProvider 额外调整 trySend 顺序（移到 when 前）
- Assistant.kt 缓存使用本地 `SimpleCache`（`CacheUtil.kt`）
- ChatService 保留 `soundEffectPlayer` 和声音反馈逻辑
- ChatNotificationManager 包名改为 `net.weero.measix.pilot`
- TTSManager getPromptGuidance 仅 4 个分支

---

### 2.9 `55c2cf71` — fix(ui): 聊天头部模型名支持两行显示

| 项目 | 内容 |
|------|------|
| 描述 | `provider/model` 格式的长模型名在头部 `maxLines = 1` 时仅显示斜杠前内容 |
| 影响文件 | `ChatMessageAvatar.kt` |
| 日期 | 2026-07-08 |

**上游 diff**：
```kotlin
// 旧：
maxLines = 1,
// 新：
maxLines = 2,
overflow = TextOverflow.Ellipsis,
```
+ 新增 `import TextOverflow`

**本地核对**：`ChatMessageAvatar.kt:109` — `maxLines = 1`，无 `overflow` ❌

**判定**：✅ **同步** — UI 修复

---

### 2.10 `8e872e5f` — fix(ui): 聊天输入框键盘收起时增加底部呼吸间距

| 项目 | 内容 |
|------|------|
| 描述 | 键盘收起时输入框底部没有呼吸间距 |
| 影响文件 | `ChatInput.kt` |
| 日期 | 2026-07-08 |

**上游 diff**：
```kotlin
// 旧：
.padding(horizontal = 8.dp),
// 新：
.padding(horizontal = 8.dp)
.padding(bottom = if (imeVisible) 0.dp else 8.dp),
```

**本地核对**：`ChatInput.kt:205` — `.padding(horizontal = 8.dp)` 无条件 bottom padding ❌
- `imeVisible` 已在 `:145` 定义 ✅

**判定**：✅ **同步** — UI 修复

---

## 三、汇总判定

| # | 提交 | 描述 | 判定 | 优先级 |
|---|------|------|------|--------|
| 1 | `6a35f89e` | TTS 语气标记引导 + MiMo | ✅ 同步 | P2 |
| 2 | `45191d27` | MiniMax 移除 emotion | ❌ 跳过 | — |
| 3 | `c9394d94` | CI 每日构建 | ❌ 跳过 | — |
| 4 | `6ce74f71` | CI submodule | ❌ 跳过 | — |
| 5 | `feeed5dc` | CI Prerelease | ❌ 跳过 | — |
| 6 | `baf72a21` | 版本升级 2.4.1 | ❌ 跳过 | — |
| 7 | `8477334d` | MCP SSE 无限重连修复 | ✅ 同步 | P0 |
| 8 | `7d88bbc3` | 流式丢字修复 + 通知拆分 | ✅ 同步 | P0 |
| 9 | `55c2cf71` | 模型名两行显示 | ✅ 同步 | P1 |
| 10 | `8e872e5f` | 输入框底部间距 | ✅ 同步 | P1 |

**跳过**：5 个提交（MiniMax TTS / CI ×3 / 版本升级）
**需同步**：5 个提交

---

## 四、同步执行计划

### 步骤 1：Provider 流式丢字修复（4 文件）

| 文件 | 改动 |
|------|------|
| `ClaudeProvider.kt` | 新增 import + trySend 移到 when 前 + onFailure + buffer(UNLIMITED) |
| `GoogleProvider.kt` | 新增 import + trySend onFailure + buffer(UNLIMITED) |
| `ChatCompletionsAPI.kt` | 新增 import + trySend onFailure + buffer(UNLIMITED) |
| `ResponseAPI.kt` | 新增 import + trySend onFailure + buffer(UNLIMITED) |

### 步骤 2：replaceRegexes 缓存（1 文件）

| 文件 | 改动 |
|------|------|
| `Assistant.kt` | 新增 import + regexCache + compileRegexCached + replaceRegexes 改用缓存 |

### 步骤 3：MCP SSE 无限重连修复（1 文件）

| 文件 | 改动 |
|------|------|
| `McpManager.kt` | transport.onError 新增过滤 + isSseStreamGiveUpError 方法 |

### 步骤 4：通知拆分（6 文件）

| 文件 | 改动 |
|------|------|
| `AppEvent.kt` | 新增 ChatGenerationUpdate + ChatGenerationEnded |
| `AppEventBus.kt` | 新增 tryEmit |
| `ChatNotificationManager.kt`（新建） | 通知管理器 |
| `ChatService.kt` | 新增 appEventBus 依赖 + 移出通知逻辑 + 保留声音反馈 |
| `RouteActivity.kt` | 新增事件分支 |
| `AppModule.kt` | 注册 ChatNotificationManager + ChatService 新增参数 + LocalTools 新增参数 |

### 步骤 5：TTS 语气标记引导（5 文件）

| 文件 | 改动 |
|------|------|
| `TTSProvider.kt` | 新增 promptGuidance 属性 |
| `TTSManager.kt` | 新增 getPromptGuidance |
| `MiMoTTSProvider.kt` | 新增 promptGuidance override |
| `TextToSpeechTool.kt` | 新增 systemPrompt 注入 |
| `LocalTools.kt` | 新增 ttsManager + settingsStore 依赖 |

### 步骤 6：UI 修复（2 文件）

| 文件 | 改动 |
|------|------|
| `ChatMessageAvatar.kt` | maxLines=2 + overflow + import |
| `ChatInput.kt` | 条件 bottom padding |

### 步骤 7：验证

- 编译通过（`./gradlew compileDebugKotlin`）
- 单元测试通过（`./gradlew test`）
- 逐文件与上游最终态核对

---

## 五、本地偏离记录

| 文件 | 偏离 | 原因 |
|------|------|------|
| `TTSManager.kt` | getPromptGuidance 仅 4 个 provider 分支 | 本地精简 TTS（OpenAI/Gemini/SystemTTS/MiMo） |
| `TTSProviderSetting.kt` | MiMo 默认模型已是 v2.5 | batch-1 引入时即为 v2.5 |
| `ChatService.kt` | 保留 soundEffectPlayer 和声音反馈逻辑 | 本地 batch-0.0.4 引入，上游无 |
| `McpManager.kt` | 仅 1 处 transport.onError | batch-3 MCP 重构后结构不同 |
| `ChatNotificationManager.kt` | 包名 net.weero.measix.pilot | 包名映射 |
| `RouteActivity.kt` | 上游 `7d88bbc3` 附带 3 处格式修复（OkHttpNetworkFetcherFactory 缩进、conversationId 行尾换行、popTransitionSpec 缩进）未应用 | 纯格式调整，不影响功能 |

---

## 六、持久化影响评估

| 改动 | 持久化影响 | 风险 |
|------|-----------|------|
| Provider buffer/trySend | 流式传输逻辑，无持久化 | **无风险** |
| replaceRegexes 缓存 | 纯内存缓存，无持久化 | **无风险** |
| MCP SSE 过滤 | 连接管理逻辑，无持久化 | **无风险** |
| ChatNotificationManager | 通知逻辑，无持久化 | **无风险** |
| AppEvent 新增类型 | 事件类型，无持久化 | **无风险** |
| TTS promptGuidance | TTSProvider 接口新增可选属性（默认空），无 @Serializable 变更 | **无风险** |
| LocalTools 新增依赖 | DI 参数变更，无持久化 | **无风险** |
| UI 修复 | 布局调整，无持久化 | **无风险** |

> 本批同步全部为代码级改动，无 Room schema 变更、无 DataStore 配置结构变更、无 @Serializable data class 结构变更、无 AndroidManifest 变更。

---

## 七、验证结果

### 编译验证

```
./gradlew compileDebugKotlin
BUILD SUCCESSFUL in 1m 53s
84 actionable tasks: 2 executed, 82 up-to-date
```

仅存在预先存在的废弃 API 警告（`ListItem` deprecation），与本批改动无关。

### 单元测试验证

```
./gradlew test
BUILD SUCCESSFUL in 36s
199 actionable tasks: 60 executed, 139 up-to-date
```

全部单元测试通过，无失败。

### 逐文件核对结果

| # | 文件 | 核对结果 |
|---|------|---------|
| 1 | `ClaudeProvider.kt` | ✅ trySend 移到 when 前 + onFailure + buffer(UNLIMITED) |
| 2 | `GoogleProvider.kt` | ✅ trySend onFailure + buffer(UNLIMITED) |
| 3 | `ChatCompletionsAPI.kt` | ✅ trySend onFailure + buffer(UNLIMITED) |
| 4 | `ResponseAPI.kt` | ✅ trySend onFailure + buffer(UNLIMITED) |
| 5 | `Assistant.kt` | ✅ regexCache + compileRegexCached + SimpleCache |
| 6 | `McpManager.kt` | ✅ isSseStreamGiveUpError 过滤（1 处 onError，上游 2 处） |
| 7 | `AppEvent.kt` | ✅ ChatGenerationUpdate + ChatGenerationEnded |
| 8 | `AppEventBus.kt` | ✅ tryEmit 方法 |
| 9 | `ChatNotificationManager.kt` | ✅ 完整通知管理器（201 行） |
| 10 | `ChatService.kt` | ✅ 通知移出 + appEventBus + 保留声音反馈 |
| 11 | `RouteActivity.kt` | ✅ 新增事件分支 |
| 12 | `AppModule.kt` | ✅ ChatNotificationManager 注册 + ChatService 参数 + LocalTools 参数 |
| 13 | `ChatMessageAvatar.kt` | ✅ maxLines=2 + overflow=Ellipsis |
| 14 | `ChatInput.kt` | ✅ 条件 bottom padding |
| 15 | `TTSProvider.kt` | ✅ promptGuidance 属性 |
| 16 | `TTSManager.kt` | ✅ getPromptGuidance（4 分支，本地精简） |
| 17 | `MiMoTTSProvider.kt` | ✅ promptGuidance override |
| 18 | `TextToSpeechTool.kt` | ✅ systemPrompt 注入 |
| 19 | `LocalTools.kt` | ✅ ttsManager + settingsStore 依赖 |

### 本地偏离说明

| 偏离 | 原因 | 影响 |
|------|------|------|
| `ChatService.kt` 保留 `isForeground`/`lifecycleObserver` | 声音反馈逻辑（batch-0.0.4 引入）需要前台状态检测 | 无 — ChatNotificationManager 有独立的 isForeground |
| `TTSManager.kt` getPromptGuidance 仅 4 个分支 | Fork 精简 TTS（OpenAI/Gemini/SystemTTS/MiMo） | 无 — 精简的 provider 均无 promptGuidance |
| `McpManager.kt` 仅 1 处 transport.onError | batch-3 MCP 重构后结构不同 | 无 — 唯一的 onError 已添加过滤 |

---

*同步完成日期：2026-07-08*
