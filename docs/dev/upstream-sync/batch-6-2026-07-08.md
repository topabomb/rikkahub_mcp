# 上游同步分析 — 第六批（2026-07-08）

> 本文档为第六批上游同步检查的完整分析记录，包含逐提交 diff 分析、本地代码核对、适配要点。
> 进度摘要见 [`docs/dev/upstream-sync.md`](../upstream-sync.md)。
>
> **同步状态：✅ 已同步完成（编译通过 + 单元测试通过 + 逐文件核对一致）**
>
> 21 个提交中 16 个需同步（bug 修复 4 + 重构 2 + 功能增强 3 + 模型注册 2 + 测试 2 + UI 修复 3），5 个跳过。
> 补充检查：上游新增 3 个提交——`a2a71da2`（MiniMax/MIMO 内置提供商，跳过）、`624ab635`（工作区导入按钮移至顶栏，已同步）、`d7f0ef26`（HEIF/HEIC 图片上传与裁剪支持，已同步）。

---

## 一、背景

### 1.1 项目概况

- **项目**：Measix Pilot — 从 [RikkaHub](https://github.com/rikkahub/rikkahub) fork 的 Android LLM 聊天客户端
- **Fork 基线**：RikkaHub 2.3.1（versionCode 164）— 2026-06-18
- **本地当前版本**：0.0.9（versionCode 9）
- **本地 DB 版本**：version = 3（含文件夹表）
- **包名映射**：上游 `me.rerere.rikkahub` → 本地 `net.weero.measix.pilot`
- **DeepLink scheme**：上游 `rikkahub://` → 本地 `measix://`

### 1.2 上次同步状态

- **上次检查点**：`ef564dca`（2026-07-05，第五批）
- **上次同步范围**：`5b39e05d..ef564dca`（2026-07-05，8 个提交，6 个已同步）
- **永久跳过**：赞助商 / 推荐提供商 / Serper 搜索 / StepFun TTS / ElevenLabs TTS

### 1.3 本次检查范围

- **检查范围**：`ef564dca..upstream/master`（2026-07-06 ~ 2026-07-08，共 **21 个提交**）
- **最新上游提交**：`624ab635`（2026-07-08，工作区导入按钮移至顶栏）
- **上游版本**：2.4.0（versionCode 168，未升级）

### 1.3.1 版本号对应关系

| 维度 | 上游 | 本地 | 说明 |
|------|------|------|------|
| 版本名 | 2.4.0 | 0.0.9 | 版本线独立，不跟随上游版本号 |
| versionCode | 168 | 9 | 独立递增 |
| DB 版本 | v24 | v3 | 本地从 v1 重建，无历史 migration 包袱 |
| MCP SDK | 0.14.0 | 0.14.0 | batch-4 已对齐 |

> 本批同步不递增本地版本号（`versionCode`/`versionName` 保持 9/0.0.9），改动以补丁级修复和重构为主。

### 1.4 核对方法

按工作方法执行：`git fetch upstream` → `git log --oneline ef564dca..upstream/master` → 逐提交 `git show <hash> --stat` + `git show <hash> -- <path>` 获取完整 diff → 打开本地对应文件逐行核对。

---

## 二、提交逐项分析

### 2.1 `4d2276d0` — fix(webview): 修复 mhchem.mjs 导入方式 (#1444)

| 项目 | 内容 |
|------|------|
| 描述 | batch-5 同步的 mhchem 扩展使用 `import katexMhchem from ...` + `katex.use(katexMhchem)` 方式导入，但 esm.sh 的 mhchem.mjs 是 side-effect 模块，直接 `import '...'` 即可自动注册到 katex。原方式可能导致重复注册或导入失败 |
| 影响文件 | `app/src/main/assets/html/mark.html` |
| 日期 | 2026-07-06 |

**上游 diff**：
```javascript
// mark.html, mhchem 导入
- import katexMhchem from 'https://esm.sh/katex@0.16.8/dist/contrib/mhchem.mjs';
- katex.use(katexMhchem);
+ import 'https://esm.sh/katex@0.16.8/dist/contrib/mhchem.mjs';
```

**本地核对**：
- `mark.html:182-183` — 仍为 `import katexMhchem from ...` + `katex.use(katexMhchem)` ❌（batch-5 同步的原始写法）

**判定**：✅ **同步** — bug 修复，修正 batch-5 引入的 mhchem 导入方式

**适配**：无（单行替换，删除 import 绑定 + `katex.use` 调用，改为 side-effect import）

---

### 2.2 `0df1939c` — fix(webview): 修复 mhchem \ce 化学公式无法渲染

| 项目 | 内容 |
|------|------|
| 描述 | batch-5 同步 mhchem 后 `\ce{}` 仍无法渲染。原因：`markdown-it-katex` 插件内部自带另一个 katex 实例，未加载 mhchem 扩展。修复：传入已加载 mhchem 的 katex 实例给插件 |
| 影响文件 | `app/src/main/assets/html/mark.html` |
| 日期 | 2026-07-07 |

**上游 diff**：
```javascript
// mark.html, markdown-it-katex 插件注册
- md.use(vscodemarkdownItKatex);
+ // 传入已加载 mhchem 扩展的 katex 实例, 否则插件会使用其内部自带的另一个 katex 实例,
+ // 导致 \ce 等 mhchem 语法无法识别 (Undefined control sequence: \ce)
+ md.use(vscodemarkdownItKatex, { katex });
```

**本地核对**：
- `mark.html:210` — 仍为 `md.use(vscodemarkdownItKatex)` 无 `{ katex }` 选项 ❌

**判定**：✅ **同步** — bug 修复，与 #1 配套完成 batch-5 mhchem 功能

**适配**：无（单行替换，添加 `{ katex }` 选项参数）

> **合并说明**：#1 和 #2 均为 batch-5 mhchem 功能的后续修复，合并最终态一次引入。

---

### 2.3 `89f8c4b1` — fix(API): 工具结果图片按模型输入模态回传

| 项目 | 内容 |
|------|------|
| 描述 | batch-4 同步的 `toToolResultContent()` 无条件将工具返回的图片/视频/音频回传给模型，但不支持图片输入的模型会报错。修复：根据 `model.inputModalities` 判断是否支持图片，不支持时以文本占位替代；同时简化为仅处理文字和图片（移除 Video/Audio） |
| 影响文件 | `ai/.../openai/ChatCompletionsAPI.kt` |
| 日期 | 2026-07-06 |

**上游 diff（4 处改动）**：

1. `buildChatCompletionRequest` — `buildMessages` 调用新增 `supportInputModalities = params.model.inputModalities` 参数：
```kotlin
put("messages", buildMessages(
    messages = messages,
    includeHistoryReasoning = providerSetting.includeHistoryReasoning,
    supportInputModalities = params.model.inputModalities,
))
```

2. `buildMessages` — 新增 `supportInputModalities: List<Modality>` 参数（默认 `listOf(Modality.TEXT, Modality.IMAGE)`），传递给 `addAssistantMessages`

3. `addAssistantMessages` — 新增 `supportInputModalities` 参数，传递给 `tool.toToolResultContent(supportInputModalities)`

4. `toToolResultContent` — 重写为带参数版本：
   - 检查 `Modality.IMAGE in supportInputModalities`，计算 `hasImageToSend = output.any { it is Image && supportsImageInput }`
   - 不支持图片或无图片时（`!hasImageToSend`）：返回 `JsonPrimitive`，使用 `mapNotNull` 遍历 output，Text 提取文本、Image 替换为 `"[Image output omitted: current model does not support image input]"`、其他类型丢弃（旧代码用 `filterIsInstance<Text>` 静默丢弃所有非文本部分，新代码保留图片占位提示）
   - 支持图片且有图片时：返回 `JsonArray`，仅处理 Text + Image（Image 分支保持 `encodeBase64().onSuccess { encodedImage -> ... encodedImage.base64 }` 逻辑不变）
   - **移除 Video 和 Audio 处理**（简化为仅文字和图片），删除对应的 `when` 分支（~30 行）

**本地核对**：
- `ChatCompletionsAPI.kt:258` — `buildMessages(messages, providerSetting.includeHistoryReasoning)` 无 `supportInputModalities` ❌
- `ChatCompletionsAPI.kt:438` — `buildMessages` 签名无 `supportInputModalities` ❌
- `ChatCompletionsAPI.kt:443` — `addAssistantMessages(message, includeReasoning = ...)` 调用处无 `supportInputModalities` ❌
- `ChatCompletionsAPI.kt:450` — `addAssistantMessages` 签名无 `supportInputModalities` ❌
- `ChatCompletionsAPI.kt:487` — `tool.toToolResultContent()` 无参数 ❌
- `ChatCompletionsAPI.kt:627-692` — `toToolResultContent()` 无参数，处理 Image + Video + Audio ❌

**前置条件确认**：
- `Modality` 已在 `ChatCompletionsAPI.kt:29` 导入 ✅
- `Model.inputModalities` 已在 `ai/.../provider/Model.kt:15` 定义为 `List<Modality>` ✅

**判定**：✅ **同步** — bug 修复，修正 batch-4 引入的多模态工具调用

**适配要点**：
- 参数传递链（4 处签名/调用变更）：`buildChatCompletionRequest:258` → `buildMessages:438+443` → `addAssistantMessages:450+487` → `toToolResultContent:627`
- `buildMessages` 新增 `supportInputModalities` 参数有默认值 `listOf(Modality.TEXT, Modality.IMAGE)`，仅有一个调用处需更新
- `toToolResultContent` 从无参改为带参，方法体重写：text-only 分支从 `filterIsInstance<Text>` 改为 `mapNotNull`（含图片占位），删除 Video/Audio `when` 分支
- 本地 batch-4 同步的 Video/Audio 处理将被移除（上游已简化）
- `Modality`、`JsonElement`、`JsonPrimitive`、`Log` 等均已导入（batch-4 已确认），无需新增 import

---

### 2.4 `8eeaa801` — fix: 修复中文弯引号无法正确匹配的问题 (#1461)

| 项目 | 内容 |
|------|------|
| 描述 | `extractQuotedContent()` 使用 Kotlin raw string `"""..."""` 包含中文弯引号（`""''`），但 raw string 中这些 Unicode 字符可能被编辑器/编译器处理为不同码点，导致正则无法匹配。改为使用 `\u201C`/`\u201D`/`\u2018`/`\u2019` Unicode 转义 |
| 影响文件 | `app/.../utils/StringUtils.kt` |
| 日期 | 2026-07-07 |

**上游 diff**：
```kotlin
// StringUtils.kt, extractQuotedContent() patterns
- """([^"]*?)""",  // 中文双引号
- """'([^']*?)'""",  // 中文单引号
+ "\u201C([^\u201D]*?)\u201D",  // 中文双引号
+ "\u2018([^\u2019]*?)\u2019",  // 中文单引号
```

**本地核对**：
- `StringUtils.kt:103-104` — 仍使用 raw string 包含中文弯引号字面量 ❌

**判定**：✅ **同步** — bug 修复

**适配**：无（2 行替换，raw string → Unicode 转义字符串）

> **关联**：本修复与 #15（`e1ef629b`）均修改 `extractQuotedContent()` 的 patterns 列表，合并最终态一次引入（共 6 个正则模式）。

---

### 2.5 `152e8614` — test: 新增 extractQuotedContent 引号提取单元测试

| 项目 | 内容 |
|------|------|
| 描述 | 为 `extractQuotedContent()` 和 `extractQuotedContentAsText()` 新增 9 个单元测试，覆盖中文双引号/单引号、英文双引号/单引号、多引号、空内容、无引号、合并输出等场景 |
| 影响文件 | `app/src/test/java/.../utils/StringUtilsTest.kt`（新建） |
| 日期 | 2026-07-07 |

**上游 diff**：新建 `StringUtilsTest.kt`（57 行），9 个测试方法。

**本地核对**：
- 本地不存在 `StringUtilsTest.kt` ❌
- `extractQuotedContent()` 和 `extractQuotedContentAsText()` 均在本地 `StringUtils.kt:99,125` 定义 ✅

**判定**：✅ **同步** — 测试配套引入（与 #4 修复配套）

**适配**：
- 新建测试文件，改包名 `me.rerere.rikkahub` → `net.weero.measix.pilot`
- 测试方法逻辑不涉及包名差异，直接引入

> **关联**：本测试与 #16（`57e6dbc1`）均修改 `StringUtilsTest.kt`，合并最终态一次引入（共 11 个测试方法）。

---

### 2.6 `b7eb6d77` — refactor: 统一文件大小智能显示为 fileSizeToString() (#1468)

| 项目 | 内容 |
|------|------|
| 描述 | 合并分散在各处的 4 份 `formatBytes`/`fileSizeToString` 实现，统一为三位有效数字 + 支持到 TB 的版本。重写 `StringUtils.kt:fileSizeToString()`，删除 3 处私有 `formatBytes()` 死代码/重复实现 |
| 影响文件 | 4 文件 |
| 日期 | 2026-07-08 |

**上游逐文件 diff 与本地核对**：

#### 文件 1：`StringUtils.kt` — `fileSizeToString()` 重写

**上游 diff**：
```kotlin
// 旧：整数除法，仅到 GB
fun Long.fileSizeToString(): String {
    return when {
        this < 1024 -> "$this B"
        this < 1024 * 1024 -> "${this / 1024} KB"
        this < 1024 * 1024 * 1024 -> "${this / (1024 * 1024)} MB"
        else -> "${this / (1024 * 1024 * 1024)} GB"
    }
}

// 新：浮点除法，三位有效数字，支持到 TB
fun Long.fileSizeToString(): String {
    if (this < 1024) return "$this B"
    val units = arrayOf("KB", "MB", "GB", "TB")
    var value = toDouble() / 1024.0
    var unitIndex = 0
    while (value >= 1024 && unitIndex < units.lastIndex) {
        value /= 1024.0
        unitIndex++
    }
    val precision = when {
        value >= 100 -> 0
        value >= 10 -> 1
        else -> 2
    }
    return "%.${precision}f %s".format(value, units[unitIndex])
}
```

**本地核对**：`StringUtils.kt:47-54` — 仍为旧整数除法版本 ❌

#### 文件 2：`WorkspaceToolUIs.kt` — 删除死代码

**上游 diff**：删除 `formatBytes()` 私有函数（7 行），注释为 "unused dead code"。

**本地核对**：`WorkspaceToolUIs.kt:430` — 存在 `formatBytes()` 私有函数，经 grep 确认在本文件内无调用 ❌

#### 文件 3：`WorkspaceDetailPage.kt` — 改用公共函数

**上游 diff**：
- 删除私有 `formatBytes()`（12 行）
- 2 处位置（3 个调用）`formatBytes(x)` → `x.fileSizeToString()`：
  - 下载进度（`:473-474`）：`formatBytes(it)` + `formatBytes(progress.bytesRead)`
  - 文件卡片（`:685`）：`formatBytes(entry.sizeBytes)`
- 新增 `import ...fileSizeToString`

**本地核对**：
- `WorkspaceDetailPage.kt:786` — 存在私有 `formatBytes()` ❌
- `WorkspaceDetailPage.kt:473-474` — 调用 `formatBytes()` ❌
- `WorkspaceDetailPage.kt:685` — 调用 `formatBytes()` ❌

#### 文件 4：`SettingFilesPage.kt` — 改用公共函数

**上游 diff**：
- 删除私有 `formatBytes()`（9 行）
- 1 处调用 `formatBytes(file.sizeBytes)` → `file.sizeBytes.fileSizeToString()`
- 新增 `import ...fileSizeToString`

**本地核对**：
- `SettingFilesPage.kt:265` — 存在私有 `formatBytes()` ❌
- `SettingFilesPage.kt:256` — 调用 `formatBytes(file.sizeBytes)` ❌

**本地偏离发现**：
- `WebDavTab.kt:470`、`S3Tab.kt:489` — 已使用 `fileSizeToString()`（本地已有，不受影响）
- `WebDavSync.kt`、`S3Sync.kt` — 已使用 `fileSizeToString()`（日志中，不受影响）
- 以上均为本地文件，上游不涉及，无需修改

**判定**：✅ **同步** — 重构改进，消除重复代码

**适配**：
- 重写 `StringUtils.kt:fileSizeToString()`
- 删除 3 处私有 `formatBytes()` + 改调用为 `fileSizeToString()`
- 新增 2 个 import（`WorkspaceDetailPage.kt` + `SettingFilesPage.kt`）

---

### 2.7 `f4be676b` — feat(model): 新增 GLM-5.2、HY3、LongCat-2.0 模型注册

| 项目 | 内容 |
|------|------|
| 描述 | ModelRegistry 新增 3 个模型定义：GLM-5.2（智谱）、HY3（阶跃星辰）、LongCat-2.0（美图），均为 toolReasoningAbility |
| 影响文件 | `ai/.../registry/ModelRegistry.kt` |
| 日期 | 2026-07-07 |

**上游 diff（3 处新增）**：

1. `GLM_5_2` 定义（在 `GLM_5_1` 后）：
```kotlin
private val GLM_5_2 = defineModel {
    tokens("glm", "5", "2")
    toolReasoningAbility()
}
```

2. `HY3` + `LONGCAT_2` 定义（在 `XIAOMI_MIMO_V2_5_PRO` 后）：
```kotlin
private val HY3 = defineModel {
    tokens("hy", "3")
    toolReasoningAbility()
}

private val LONGCAT_2 = defineModel {
    tokens("longcat", "2", "0")
    toolReasoningAbility()
}
```

3. 模型列表新增 `GLM_5_2`、`HY3`、`LONGCAT_2`

**本地核对**：
- 本地 ModelRegistry 不存在 `GLM_5_2`、`HY3`、`LONGCAT_2` ❌
- 本地 `GLM_5_1` 在 `ModelRegistry.kt:393`，`XIAOMI_MIMO_V2_5_PRO` 在 `:435`
- 本地模型列表末尾为 `XIAOMI_MIMO_V2_5_PRO,` → `)`（无 `QWEN_MT`，本地 fork 未引入）

**判定**：✅ **同步** — 模型注册，纯增量

**适配**：
- 在 `GLM_5_1` 后插入 `GLM_5_2`
- 在 `XIAOMI_MIMO_V2_5_PRO` 后插入 `HY3` + `LONGCAT_2`
- 模型列表对应位置新增 3 个条目
- ⚠️ 本地无 `QWEN_MT`，`HY3`/`LONGCAT_2` 插入位置为 `XIAOMI_MIMO_V2_5_PRO` 后、`)` 前

---

### 2.8 `21d04284` — feat: broaden Qwen 3.5-3.7 and Doubao seed-2 model capability detection (#1466)

| 项目 | 内容 |
|------|------|
| 描述 | 新增 Qwen 3.5/3.6/3.7 MAX 变体（无 vision，正确排除 -max 变体的 vision 检测，higher token score wins）+ Doubao 2.0/2.1（vision + tool + reasoning） |
| 影响文件 | `ai/.../registry/ModelRegistry.kt` |
| 日期 | 2026-07-08 |

**上游 diff（3 处新增）**：

1. `QWEN_3_5_MAX` / `QWEN_3_6_MAX` / `QWEN_3_7_MAX` 定义（在 `QWEN_3_7` 后、`DOUBAO_1_6` 前）：
```kotlin
private val QWEN_3_5_MAX = defineModel {
    tokens("qwen", "3", "5", "max")
    toolReasoningAbility()
}
// ... QWEN_3_6_MAX, QWEN_3_7_MAX 同理
```

2. `DOUBAO_2_0` / `DOUBAO_2_1` 定义（在 `DOUBAO_1_8` 后）：
```kotlin
private val DOUBAO_2_0 = defineModel {
    tokens("doubao", "2", "0")
    visionInput()
    toolReasoningAbility()
}
// ... DOUBAO_2_1 同理
```

3. 模型列表新增 5 个条目

**本地核对**：
- 本地 ModelRegistry 不存在 `QWEN_3_5_MAX` 等 5 个模型 ❌
- 本地 `QWEN_3_7` 在 `:314`，`DOUBAO_1_6` 在 `:320`，`DOUBAO_1_8` 在 `:326`

**判定**：✅ **同步** — 模型注册，纯增量

**适配**：
- 在 `QWEN_3_7` 后插入 3 个 MAX 变体
- 在 `DOUBAO_1_8` 后插入 2 个 Doubao 2.x
- 模型列表对应位置新增 5 个条目

> **合并说明**：#7 和 #8 均修改 `ModelRegistry.kt`，合并最终态一次引入，共新增 8 个模型定义 + 8 个列表条目。

---

### 2.9 `1f937b8d` — refactor: 移除 Google Imagen 图像生成支持

| 项目 | 内容 |
|------|------|
| 描述 | 将 `Provider.generateImage` 改为带默认 `error()` 实现，删除 `GoogleProvider` 的 Imagen predict 接口实现及相关 import。OpenAI/Claude Provider 的 `generateImage` 不受影响（仍 override 默认实现） |
| 影响文件 | `ai/.../provider/Provider.kt`、`ai/.../provider/providers/GoogleProvider.kt` |
| 日期 | 2026-07-08 |

**上游 diff（2 文件）**：

#### 文件 1：`Provider.kt` — 添加默认实现

```kotlin
// 旧（abstract）：
suspend fun generateImage(
    providerSetting: ProviderSetting,
    params: ImageGenerationParams,
): Flow<ImageGenerationItem>

// 新（default error）：
suspend fun generateImage(
    providerSetting: ProviderSetting,
    params: ImageGenerationParams,
): Flow<ImageGenerationItem> {
    error("Image generation is not supported")
}
```

#### 文件 2：`GoogleProvider.kt` — 删除 Imagen 实现 + 清理 import

- 删除 `generateImage` override 方法（~82 行）
- 删除 4 个仅在该方法中使用的 import：
  - `import kotlinx.serialization.json.putJsonObject`
  - `import me.rerere.ai.provider.ImageGenerationParams`
  - `import me.rerere.ai.ui.ImageAspectRatio`
  - `import me.rerere.ai.ui.ImageGenerationItem`

**本地核对**：
- `Provider.kt:41-44` — `generateImage` 为 abstract（无方法体）❌
- `GoogleProvider.kt:801-864` — 存在完整 Imagen 实现 ❌
- `GoogleProvider.kt:26` — `import putJsonObject`（仅用于 generateImage 的 `putJsonObject("parameters")`）❌
- `GoogleProvider.kt:31` — `import ImageGenerationParams`（仅用于 generateImage 参数）❌
- `GoogleProvider.kt:42-43` — `import ImageAspectRatio` / `ImageGenerationItem`（仅用于 generateImage）❌
- `OpenAIProvider.kt:204` — `override suspend fun generateImage`（不受影响，仍 override 默认实现）✅
- `ClaudeProvider.kt:99` — `override suspend fun generateImage`（不受影响）✅

**判定**：✅ **同步** — 架构改进，移除不再维护的 Google Imagen 支持

**适配**：
- `Provider.kt` — 添加默认 `error()` 方法体
- `GoogleProvider.kt` — 删除 `generateImage` 方法 + 4 个 import
- 验证 `putJsonObject` 在 GoogleProvider 中无其他使用（已确认仅 `:816` 一处，在 generateImage 内）

---

### 2.10 `d1aa55e5` — feat(search): add ollama fetch (#1442)

| 项目 | 内容 |
|------|------|
| 描述 | OllamaSearchService 新增 scraping（网页抓取）能力，调用 `https://ollama.com/api/web_fetch` API 获取网页内容 |
| 影响文件 | `search/.../OllamaSearchService.kt` |
| 日期 | 2026-07-06 |

**本地核对**：
- 本地 search 模块不存在 `OllamaSearchService`（grep 确认无 `Ollama`/`ollama` 匹配）❌

**判定**：❌ **跳过** — Ollama 搜索引擎未在 Fork 中引入（AGENTS.md 列出的搜索引擎为 Bing、Tavily、SearXNG、Custom JS，不含 Ollama）

---

### 2.11 `0aa8073b` — feat: 新增 fish audio TTS 提供商 (#1451)

| 项目 | 内容 |
|------|------|
| 描述 | 新增 FishAudio TTS provider（参照 ElevenLabs 风格），支持 s2.1-pro-free 免费模型 |
| 影响文件 | 10 文件，+308 行 |
| 日期 | 2026-07-07 |

**判定**：❌ **跳过** — Fork 精简 TTS Provider，不引入新 TTS（永久跳过汇总已记录 StepFun TTS、ElevenLabs TTS 同类跳过）

---

### 2.12 `5856325d` — build(web): 支持跨平台构建 web-ui

| 项目 | 内容 |
|------|------|
| 描述 | 根据 OS 选择构建命令，Windows 使用 `cmd /c` 避免 `pnpm.cmd` 找不到 |
| 影响文件 | `web/build.gradle.kts` |
| 日期 | 2026-07-07 |

**判定**：❌ **跳过** — Fork 未引入 web 模块（无 `web/` 目录）

---

### 2.13 `70aa7a0c` — feat(web): 新增 web 端文件夹功能与合并 SSE 事件流

| 项目 | 内容 |
|------|------|
| 描述 | 后端新增 `folderRoutes`（文件夹 CRUD）+ `EventsRoutes`（SSE 多路复用），前端新增文件夹筛选与实时同步 |
| 影响文件 | 22 文件，+1791 -895 行 |
| 日期 | 2026-07-07 |

**本地核对**：
- Fork 未引入 web 模块（无 `web/` 目录、无 `WebServerManager`、无 `WebApiModule`）
- 后端 `ConversationRepository.kt` 新增 `getUnfiledConversationsOfAssistantPage` / `getConversationsOfFolderPage` / `loadConversationPage` — 仅用于 web API 分页
- 后端 `FolderRepository.kt` 新增 `getFolderById` — 仅用于 web API
- 后端 `AppModule.kt` — 为 `WebServerManager` 添加 `folderRepo` 参数（本地无 WebServerManager）
- 移动端文件夹功能已在 batch-4 完整同步（ChatDrawer 等），本提交的 web 端功能不适用

**判定**：❌ **跳过** — web 模块未引入，后端变更为 web API 专用

---

### 2.14 `d07b54ca` — feat(web-ui): 重构推理强度选择器为紧凑滑块样式

| 项目 | 内容 |
|------|------|
| 描述 | 参考 Claude.ai 风格重新设计推理选择器弹窗 |
| 影响文件 | `web-ui/app/components/input/reasoning-picker.tsx` + 2 个 locale 文件 |
| 日期 | 2026-07-07 |

**判定**：❌ **跳过** — web-ui 模块未引入

---

### 2.15 `0217ca82` — feat(imggen): 图像尺寸改用 ImageGenSize 并支持自定义输入

| 项目 | 内容 |
|------|------|
| 描述 | 将 `ImageAspectRatio` 枚举替换为携带实际 size 值的 `ImageGenSize` 枚举，`ImageGenerationParams`/`ImageEditParams` 的 `aspectRatio` 字段改为 `size: String`，空值时不下发 size 让 API 使用默认值；设置面板新增自定义尺寸输入框，并让底部弹窗跳过半展开状态 |
| 影响文件 | 5 文件 |
| 日期 | 2026-07-08 |

**上游逐文件 diff 与本地核对**：

#### 文件 1：`Image.kt` — 枚举重命名

```kotlin
// 旧：
enum class ImageAspectRatio { SQUARE, LANDSCAPE, PORTRAIT }

// 新：
enum class ImageGenSize(val value: String) {
    AUTO("auto"), SQUARE_1024("1024x1024"), LANDSCAPE_1536("1536x1024"),
    PORTRAIT_1536("1024x1536"), SQUARE_256("256x256"), SQUARE_512("512x512"),
    LANDSCAPE_1792("1792x1024"), PORTRAIT_1792("1024x1792"),
}
```

**本地核对**：`Image.kt:14-18` — 仍为 `ImageAspectRatio` ❌

#### 文件 2：`Provider.kt` — 参数类型变更

- `import ImageAspectRatio` → `import ImageGenSize`
- `ImageGenerationParams.aspectRatio: ImageAspectRatio` → `size: String = ImageGenSize.AUTO.value`
- `ImageEditParams.aspectRatio: ImageAspectRatio` → `size: String = ImageGenSize.AUTO.value`

**本地核对**：`Provider.kt:8,73,85` — 仍使用 `ImageAspectRatio` ❌（注意：`Provider.kt` 同时被 #9 `1f937b8d` 修改 `generateImage` 默认实现，两处变更需合并）

#### 文件 3：`OpenAIProvider.kt` — 请求体构建简化

- 删除 `import ImageAspectRatio`
- `generateImage` 和 `editImage` 中的 `when (params.aspectRatio) { ... }` 硬编码 size 映射替换为 `if (params.size.isNotBlank()) { put("size", params.size) }`

**本地核对**：`OpenAIProvider.kt:27,220-223,270-273` — 仍使用 `ImageAspectRatio` when 表达式 ❌

#### 文件 4：`ImgGenPage.kt` — UI 重构

- `import ImageAspectRatio` → `import ImageGenSize`
- `aspectRatio` 状态 → `size` 状态
- `sheetState` 新增 `enabledValues = setOf(SheetValue.Hidden, SheetValue.Expanded)`（跳过半展开）
- `SettingsBottomSheet` 参数 `aspectRatio: ImageAspectRatio` → `size: String`
- 比例选择器改为 `ImageGenSize.entries.forEach` + `FilterChip`，标签直接用 `sizeOption.value`
- 新增 `OutlinedTextField` 自定义尺寸输入

**本地核对**：`ImgGenPage.kt` — 仍使用 `ImageAspectRatio` ❌

#### 文件 5：`ImgGenVM.kt` — 状态重构

- `import ImageAspectRatio` → `import ImageGenSize`
- `_aspectRatio: MutableStateFlow<ImageAspectRatio>` → `_size: MutableStateFlow<String>`
- `updateAspectRatio()` → `updateSize()`
- `ImageGenerationParams`/`ImageEditParams` 构造参数 `aspectRatio =` → `size =`

**本地核对**：`ImgGenVM.kt:24,71-72,105-106,160,209` — 仍使用 `ImageAspectRatio` ❌

**判定**：✅ **同步** — 功能增强，本地有完整 imggen 模块

**适配**：
- `Image.kt` — 枚举重命名（`ImageAspectRatio` → `ImageGenSize`，新增 `value` 属性）
- `Provider.kt` — import + 2 个字段类型变更（与 #9 的 `generateImage` 默认实现合并）
- `OpenAIProvider.kt` — import + 2 处请求体构建简化
- `ImgGenPage.kt` — import + 状态/参数/UI 重构
- `ImgGenVM.kt` — import + 状态/方法重命名

---

### 2.16 `c66935f2` — refactor(imggen): 移除设置面板中重复的模型选择器

| 项目 | 内容 |
|------|------|
| 描述 | `SettingsBottomSheet` 中的 `ModelSelector` 与页面顶部的模型选择器重复，移除设置面板中的模型选择 `FormItem` 及 `settings` 参数 |
| 影响文件 | `ImgGenPage.kt` |
| 日期 | 2026-07-08 |

**上游 diff**：
- `SettingsBottomSheet` 调用处删除 `settings = settings` 参数
- `SettingsBottomSheet` 签名删除 `settings: Settings` 参数
- 删除模型选择 `FormItem`（含 `ModelSelector`，~20 行）

**本地核对**：
- `ImgGenPage.kt:318` — `SettingsBottomSheet` 调用含 `settings = settings` ❌
- `ImgGenPage.kt:685` — `SettingsBottomSheet` 签名含 `settings: Settings` ❌
- `ImgGenPage.kt:710-727` — 存在模型选择 `FormItem` ❌

**判定**：✅ **同步** — 重构改进，消除重复 UI

**适配**：无（依赖 #15 的 `ImgGenPage.kt` 最终态，删除 `settings` 参数和模型选择器代码块）

> **合并说明**：#15 和 #16 均修改 `ImgGenPage.kt`，合并最终态一次引入。

---

### 2.17 `e1ef629b` — feat: 增加TTS朗读设置中引号所包含的标点符号 (#1460)

| 项目 | 内容 |
|------|------|
| 描述 | `extractQuotedContent()` 新增直角引号 `「」` 和白直角引号 `『』` 的正则匹配模式，扩展 TTS 朗读支持的引号类型；同步更新 strings.xml 中 TTS 引号描述文案 |
| 影响文件 | `StringUtils.kt`、`values/strings.xml`、`values-zh/strings.xml` |
| 日期 | 2026-07-08 |

**上游 diff**：

1. `StringUtils.kt` — patterns 列表新增 2 项 + 注释更新：
```kotlin
+ """「([^」]*?)」""",           // 直角引号
+ """『([^』]*?)』""",           // 白直角引号
```

2. `values/strings.xml` — 描述文案更新：
```xml
- When enabled, TTS will only read content within quotation marks
+ When enabled, TTS will only read content within quotation marks (including "", '', 「」, 『』)
```

3. `values-zh/strings.xml` — 中文描述文案更新：
```xml
- 启用后，TTS 仅朗读引号内的内容
+ 启用后，TTS 仅朗读引号（包括 ""、''、「」、『』）内的内容
```

**本地核对**：
- `StringUtils.kt:110-115` — patterns 仅 4 项，缺少直角引号 ❌
- `values/strings.xml:674` — 旧描述文案 ❌
- `values-zh/strings.xml:673` — 旧描述文案 ❌

**判定**：✅ **同步** — 功能增强，扩展 TTS 引号支持

**适配**：
- `StringUtils.kt` — patterns 列表新增 2 行 + 注释更新（与 #4 弯引号修复合并最终态）
- `values/strings.xml` + `values-zh/strings.xml` — 描述文案更新

---

### 2.18 `57e6dbc1` — test: 补充直角引号与白直角引号的提取测试

| 项目 | 内容 |
|------|------|
| 描述 | 为 `extractQuotedContent()` 新增 2 个测试用例，覆盖直角引号 `「」` 和白直角引号 `『』` 场景 |
| 影响文件 | `StringUtilsTest.kt` |
| 日期 | 2026-07-08 |

**上游 diff**：`StringUtilsTest.kt` 新增 2 个测试方法（+10 行）：
```kotlin
@Test
fun `extract corner brackets`() {
    assertEquals(listOf("你好"), "他说「你好」".extractQuotedContent())
}

@Test
fun `extract white corner brackets`() {
    assertEquals(listOf("世界"), "标题是『世界』".extractQuotedContent())
}
```

**本地核对**：`StringUtilsTest.kt` 不含上述 2 个测试 ❌

**判定**：✅ **同步** — 测试配套引入（与 #17 功能增强配套）

**适配**：无（在 #5 的 9 个测试基础上新增 2 个，共 11 个测试方法）

---

### 2.19 `5f5f9654` — fix(ui): 修复助手详情与设置页面的状态栏沉浸

| 项目 | 内容 |
|------|------|
| 描述 | 将 `innerPadding` 移入滚动容器内部（Column 内边距或 LazyColumn/Grid 的 contentPadding），使内容能够滚动到顶栏/系统栏下方实现沉浸效果。同时为 McpPicker 增加 `contentPadding` 参数，并将 AssistantMcpPage 的 TopAppBar 统一为 LargeFlexibleTopAppBar |
| 影响文件 | 9 文件 |
| 日期 | 2026-07-08 |

**上游逐文件 diff 与本地核对**：

#### 文件 1：`McpPicker.kt` — 新增 contentPadding 参数

**上游 diff**：
```kotlin
// 新增 import
+ import androidx.compose.foundation.layout.PaddingValues

// 函数签名新增参数
  fun McpPicker(
      assistant: Assistant,
      servers: List<McpServerConfig>,
      modifier: Modifier = Modifier,
+     contentPadding: PaddingValues = PaddingValues(0.dp),
      onUpdateAssistant: (Assistant) -> Unit
  ) {
      LazyColumn(
          modifier = modifier.fillMaxSize(),
+         contentPadding = contentPadding,
          verticalArrangement = Arrangement.spacedBy(4.dp),
```

**本地核对**：`McpPicker.kt:179-186` — 无 `contentPadding` 参数 ❌

#### 文件 2~7：助手详情页（6 个文件）— innerPadding 移入滚动容器

**统一模式**：`AssistantBasicPage` / `AssistantLocalToolPage` / `AssistantMemoryPage` / `AssistantPromptPage` / `AssistantRequestPage` 均做相同重构：

- Content 函数签名：`modifier: Modifier = Modifier` → `innerPadding: PaddingValues`
- 调用处：`modifier = Modifier.padding(innerPadding)` → `innerPadding = innerPadding`
- Column 修饰符：从 `modifier.fillMaxSize().padding(16.dp).verticalScroll(...)` 改为 `Modifier.fillMaxSize().verticalScroll(...).padding(horizontal = 16.dp).padding(innerPadding)`

**本地核对**：
- `AssistantBasicPage.kt:87` — `modifier = Modifier.padding(innerPadding)` ❌
- `AssistantLocalToolPage.kt:66` — `modifier = Modifier.padding(innerPadding)` ❌
- `AssistantMemoryPage.kt:84` — `modifier = Modifier.padding(innerPadding)` ❌
- `AssistantPromptPage.kt:126` — `modifier = Modifier.padding(innerPadding)` ❌
- `AssistantRequestPage.kt:56` — `modifier = Modifier.padding(innerPadding)` ❌

#### 文件 3：`AssistantMcpPage.kt` — TopAppBar → LargeFlexibleTopAppBar + McpPicker contentPadding

**上游 diff**：
- `import TopAppBar` → `import LargeFlexibleTopAppBar` + `TopAppBarDefaults` + `nestedScroll` + `LocalLayoutDirection` + `CustomColors`
- 新增 `scrollBehavior = TopAppBarDefaults.exitUntilCollapsedScrollBehavior()`
- `TopAppBar` → `LargeFlexibleTopAppBar`，新增 `scrollBehavior` + `colors` 参数
- Scaffold 新增 `modifier = Modifier.nestedScroll(scrollBehavior.nestedScrollConnection)` + `containerColor`
- McpPicker 调用：从 `modifier.padding(innerPadding).padding(16.dp)` 改为 `contentPadding = PaddingValues(...)` 使用 `calculateStartPadding`/`calculateEndPadding`

**本地核对**：
- `AssistantMcpPage.kt:7` — `import TopAppBar` ❌
- `AssistantMcpPage.kt:32` — `TopAppBar(` ❌
- `LargeFlexibleTopAppBar` 已在本地其他页面使用（`StatsPage.kt`、`SettingAboutPage.kt`、`SettingMcpPage.kt`）✅

#### 文件 8：`SettingFilesPage.kt` — innerPadding 拆分到 Column + Grid contentPadding

**上游 diff**：
- 新增 `import calculateEndPadding` / `calculateStartPadding` / `LocalLayoutDirection`
- Column 的 `.padding(innerPadding)` → `.padding(top = ..., start = ..., end = ...)`（仅 top/start/end，不含 bottom，让底部可沉浸）
- `LazyVerticalStaggeredGrid` 的 `contentPadding = PaddingValues(16.dp)` → 含 `bottom = innerPadding.calculateBottomPadding() + 16.dp`

**本地核对**：
- `SettingFilesPage.kt:124` — `.padding(innerPadding)` ❌
- `SettingFilesPage.kt` — 无 `calculateStartPadding` / `calculateEndPadding` / `LocalLayoutDirection` import ❌
- ⚠️ 本文件同时被步骤 3（fileSizeToString 重构）修改，两处变更需合并

#### 文件 9：`SettingMcpPage.kt` — PullToRefreshBox 移除 padding，LazyColumn contentPadding 计算

**上游 diff**：
- 新增 `import calculateEndPadding` / `calculateStartPadding` / `LocalLayoutDirection`
- `PullToRefreshBox` 的 `modifier = Modifier.padding(innerPadding)` → `modifier = Modifier.fillMaxSize()`
- `LazyColumn` 的 `contentPadding = PaddingValues(16.dp)` → 含 innerPadding 各方向计算的 `PaddingValues`

**本地核对**：
- `SettingMcpPage.kt:248` — `modifier = Modifier.padding(innerPadding)` ❌
- `SettingMcpPage.kt:254` — `contentPadding = PaddingValues(16.dp)` ❌
- `SettingMcpPage.kt` — 无 `calculateStartPadding` / `calculateEndPadding` / `LocalLayoutDirection` import ❌
- ⚠️ 本地偏离：`loading` 计算逻辑更复杂（batch-3 MCP 生命周期，含 Dormant/Authorizing 状态），不受影响，保留

**判定**：✅ **同步** — UI 修复，状态栏沉浸效果改进

**适配**：
- `McpPicker.kt` — 新增 `contentPadding` 参数 + import
- 6 个助手详情页 — 签名从 `modifier` 改为 `innerPadding`，padding 移入滚动容器内部
- `AssistantMcpPage.kt` — `TopAppBar` → `LargeFlexibleTopAppBar` + nestedScroll + McpPicker contentPadding
- `SettingFilesPage.kt` — innerPadding 拆分 + Grid contentPadding（与步骤 3 fileSizeToString 合并）
- `SettingMcpPage.kt` — PullToRefreshBox 移除 padding + LazyColumn contentPadding（保留本地 loading 偏离）

---

### 2.20 `5ea0e35f` — fix(ui): 点击扩展管理按钮后立即关闭底部面板

| 项目 | 内容 |
|------|------|
| 描述 | 导航回调改为直接关闭内外两层 sheet 并立即跳转，去掉等待内层收起动画的挂起等待，消除上一层面板残留约 1s 的视觉割裂 |
| 影响文件 | `FilesPicker.kt` |
| 日期 | 2026-07-08 |

**上游 diff**：
1. `InjectionQuickConfigSheet` 调用处新增 `onDismissAll = onDismiss` 参数
2. `InjectionQuickConfigSheet` 签名新增 `onDismissAll: () -> Unit` 参数
3. 删除 `import rememberCoroutineScope` + `import kotlinx.coroutines.launch`
4. 删除 `val scope = rememberCoroutineScope()`
5. 3 个导航回调（`onNavigateToQuickMessages` / `onNavigateToPrompts` / `onNavigateToSkills`）：
   ```kotlin
   // 旧：
   scope.launch {
       sheetState.hide()
       onDismiss()
       navController.navigate(Screen.XXX)
   }
   // 新：
   onDismissAll()
   navController.navigate(Screen.XXX)
   ```

**本地核对**：
- `FilesPicker.kt:288` — `onDismiss = { onShowInjectionSheetChange(false) }`，无 `onDismissAll` ❌
- `FilesPicker.kt:395` — `onDismiss: () -> Unit` 签名，无 `onDismissAll` ❌
- `FilesPicker.kt:398` — `val scope = rememberCoroutineScope()` ❌
- `FilesPicker.kt:418-437` — 3 个导航回调使用 `scope.launch { sheetState.hide(); onDismiss(); navController.navigate(...) }` ❌
- `FilesPicker.kt:40,55` — `import rememberCoroutineScope` + `import kotlinx.coroutines.launch` ❌

**判定**：✅ **同步** — UI 修复，消除面板残留视觉割裂

**适配**：
- 新增 `onDismissAll` 参数到 `InjectionQuickConfigSheet` 调用和签名
- 删除 `scope` / `rememberCoroutineScope` / `launch` import
- 3 个导航回调简化为 `onDismissAll()` + `navController.navigate(...)`

---

## 三、汇总判定

| # | 提交 | 描述 | 判定 | 优先级 |
|---|------|------|------|--------|
| 1 | `4d2276d0` | mhchem.mjs 导入方式修复 | ✅ 同步 | P0 |
| 2 | `0df1939c` | mhchem \ce 化学公式渲染修复 | ✅ 同步 | P0 |
| 3 | `89f8c4b1` | 工具结果图片按模型输入模态回传 | ✅ 同步 | P0 |
| 4 | `8eeaa801` | 中文弯引号匹配修复 | ✅ 同步 | P0 |
| 5 | `152e8614` | extractQuotedContent 单元测试 | ✅ 同步 | P1 |
| 6 | `b7eb6d77` | fileSizeToString 统一重构 | ✅ 同步 | P1 |
| 7 | `f4be676b` | GLM-5.2/HY3/LongCat-2.0 模型注册 | ✅ 同步 | P1 |
| 8 | `21d04284` | Qwen MAX/Doubao 2.x 模型检测 | ✅ 同步 | P1 |
| 9 | `1f937b8d` | 移除 Google Imagen 图像生成 | ✅ 同步 | P2 |
| 10 | `d1aa55e5` | Ollama fetch 搜索 | ❌ 跳过 | — |
| 11 | `0aa8073b` | Fish Audio TTS | ❌ 跳过 | — |
| 12 | `5856325d` | web 跨平台构建 | ❌ 跳过 | — |
| 13 | `70aa7a0c` | web 文件夹 + SSE | ❌ 跳过 | — |
| 14 | `d07b54ca` | web-ui 推理选择器 | ❌ 跳过 | — |
| 15 | `0217ca82` | imggen ImageGenSize 重构 | ✅ 同步 | P1 |
| 16 | `c66935f2` | 移除重复模型选择器 | ✅ 同步 | P1 |
| 17 | `e1ef629b` | TTS 引号扩展（直角引号） | ✅ 同步 | P1 |
| 18 | `57e6dbc1` | 直角引号提取测试 | ✅ 同步 | P1 |
| 19 | `5f5f9654` | 状态栏沉浸修复（助手详情/设置页） | ✅ 同步 | P1 |
| 20 | `5ea0e35f` | 扩展管理按钮关闭面板修复 | ✅ 同步 | P1 |
| 21 | `624ab635` | 工作区导入按钮移至顶栏 | ✅ 同步 | P1 |

**跳过**：5 个提交（Ollama 搜索 / Fish Audio TTS / web 模块 ×3）
**补充跳过**：1 个提交（`a2a71da2` MiniMax/MIMO 内置提供商 — 新 Provider，与 Fork 精简方向冲突）
**补充同步**：1 个提交（`624ab635` 工作区导入按钮移至顶栏 — UI 修复）
**需同步**：16 个提交（#1~#9 + #15~#21），其中 #1+#2 合并、#4+#17 合并、#5+#18 合并、#7+#8 合并、#15+#16 合并

---

## 四、同步执行计划

全部 16 个提交按依赖关系和风险级别分为 11 步执行（步骤 1~3 为低风险前端/工具修复，步骤 4 为重构，步骤 5~6 为 API/模型/Provider 变更，步骤 7~8 为 imggen 重构 + i18n + 字符串清理，步骤 9~10 为 UI 沉浸/面板修复，步骤 11 为验证）：

### 步骤 1：mark.html mhchem 修复（2 个提交合并）

| # | 提交 | 文件 | 改动 |
|---|------|------|------|
| 1+2 | `4d2276d0` + `0df1939c` | `mark.html` | 2 处改动（导入方式 + 插件参数） |

**改动 1**（`4d2276d0`）— `mark.html:182-183`：
```javascript
// 旧：
import katexMhchem from 'https://esm.sh/katex@0.16.8/dist/contrib/mhchem.mjs';
katex.use(katexMhchem);
// 新：
import 'https://esm.sh/katex@0.16.8/dist/contrib/mhchem.mjs';
```

**改动 2**（`0df1939c`）— `mark.html:210`：
```javascript
// 旧：
md.use(vscodemarkdownItKatex);
// 新（含上游注释）：
// 传入已加载 mhchem 扩展的 katex 实例, 否则插件会使用其内部自带的另一个 katex 实例,
// 导致 \ce 等 mhchem 语法无法识别 (Undefined control sequence: \ce)
md.use(vscodemarkdownItKatex, { katex });
```

### 步骤 2：StringUtils 修复 + TTS 引号扩展 + 测试（4 个提交合并）

| # | 提交 | 文件 | 改动 |
|---|------|------|------|
| 4+17 | `8eeaa801` + `e1ef629b` | `StringUtils.kt` | raw string → Unicode 转义（2 行）+ 新增直角引号模式（2 行）+ 注释更新 |
| | `e1ef629b` | `values/strings.xml` | TTS 引号描述文案更新 |
| | `e1ef629b` | `values-zh/strings.xml` | TTS 引号描述文案更新（中文） |
| 5+18 | `152e8614` + `57e6dbc1` | `StringUtilsTest.kt`（新建） | 11 个测试方法（9 + 2 直角引号） |

### 步骤 3：fileSizeToString 统一重构（1 个提交）

| # | 提交 | 文件 | 改动 |
|---|------|------|------|
| 6 | `b7eb6d77` | `StringUtils.kt` | 重写 `fileSizeToString()` |
| | | `WorkspaceToolUIs.kt` | 删除死代码 `formatBytes()` |
| | | `WorkspaceDetailPage.kt` | 删除 `formatBytes()` + 改调用 + 新增 import |
| | | `SettingFilesPage.kt` | 删除 `formatBytes()` + 改调用 + 新增 import |

### 步骤 4：模型注册（2 个提交合并）

| # | 提交 | 文件 | 改动 |
|---|------|------|------|
| 7+8 | `f4be676b` + `21d04284` | `ModelRegistry.kt` | 新增 8 个模型定义 + 8 个列表条目 |

### 步骤 5：ChatCompletionsAPI 工具结果修复（1 个提交）

| # | 提交 | 文件 | 改动 |
|---|------|------|------|
| 3 | `89f8c4b1` | `ChatCompletionsAPI.kt` | 6 处编辑（4 个方法签名/调用 + 方法体重写） |

**具体改动**：

1. **`:258`** — `buildMessages` 调用新增 `supportInputModalities = params.model.inputModalities`
2. **`:438`** — `buildMessages` 签名新增 `supportInputModalities: List<Modality> = listOf(Modality.TEXT, Modality.IMAGE)`
3. **`:443`** — `addAssistantMessages` 调用新增 `supportInputModalities = supportInputModalities`
4. **`:450`** — `addAssistantMessages` 签名新增 `supportInputModalities: List<Modality>`
5. **`:487`** — `tool.toToolResultContent()` → `tool.toToolResultContent(supportInputModalities)`
6. **`:627-692`** — `toToolResultContent` 方法体重写：
   - 签名从 `()` 改为 `(supportInputModalities: List<Modality>)`，返回类型仍为 `JsonElement`
   - 新增 `supportsImageInput` / `hasImageToSend` 条件判断
   - text-only 分支：`filterIsInstance<Text>` → `mapNotNull`（Text 提取文本 + Image 占位提示）
   - 删除 Video 和 Audio 的 `when` 分支（~30 行）
   - Image 分支保持不变（`encodeBase64().onSuccess { encodedImage -> ... encodedImage.base64 }`）

### 步骤 6：Google Imagen 移除（1 个提交）

| # | 提交 | 文件 | 改动 |
|---|------|------|------|
| 9 | `1f937b8d` | `Provider.kt` | 添加默认 `error()` 方法体 |
| | | `GoogleProvider.kt` | 删除 `generateImage` + 4 个 import |

### 步骤 7：imggen ImageGenSize 重构 + 移除重复模型选择器（2 个提交合并）

| # | 提交 | 文件 | 改动 |
|---|------|------|------|
| 15+16 | `0217ca82` + `c66935f2` | `Image.kt` | `ImageAspectRatio` → `ImageGenSize` 枚举重命名 |
| | | `Provider.kt` | import + `aspectRatio` → `size: String` 字段变更（与步骤 6 的 `generateImage` 默认实现合并） |
| | | `OpenAIProvider.kt` | import + 2 处请求体构建简化 |
| | | `ImgGenPage.kt` | import + 状态/参数/UI 重构 + 移除重复模型选择器 + `settings` 参数删除 |
| | | `ImgGenVM.kt` | import + 状态/方法重命名 |

### 步骤 8：i18n 修复 + 废弃字符串清理（本地改进）

| 文件 | 改动 |
|------|------|
| `ImgGenPage.kt` | 3 处硬编码字符串改为 `stringResource()` 调用 |
| 5 个 `values*/strings.xml` | 新增 `imggen_page_image_size` / `imggen_page_custom_size` / `imggen_page_custom_size_placeholder`（3 条 × 5 locale = 15 条） |
| 5 个 `values*/strings.xml` | 删除废弃的 `imggen_page_aspect_ratio*`（5 条）+ `imggen_page_model_selection*`（2 条），共 7 条 × 5 locale = 35 条 |

**说明**：上游 `0217ca82` 和 `c66935f2` 在 imggen 重构后遗留了硬编码字符串和废弃字符串资源，本地主动修复。

### 步骤 9：状态栏沉浸修复（1 个提交）

| # | 提交 | 文件 | 改动 |
|---|------|------|------|
| 19 | `5f5f9654` | `McpPicker.kt` | 新增 `contentPadding` 参数 + import |
| | | `AssistantBasicPage.kt` | `modifier` → `innerPadding`，padding 移入滚动容器 |
| | | `AssistantLocalToolPage.kt` | 同上 |
| | | `AssistantMemoryPage.kt` | 同上 |
| | | `AssistantPromptPage.kt` | 同上 |
| | | `AssistantRequestPage.kt` | 同上 |
| | | `AssistantMcpPage.kt` | `TopAppBar` → `LargeFlexibleTopAppBar` + nestedScroll + McpPicker contentPadding |
| | | `SettingFilesPage.kt` | innerPadding 拆分 + Grid contentPadding（与步骤 3 fileSizeToString 合并） |
| | | `SettingMcpPage.kt` | PullToRefreshBox 移除 padding + LazyColumn contentPadding（保留本地 loading 偏离） |

**具体适配要点**：
- 6 个助手详情页统一模式：Content 函数签名 `modifier: Modifier` → `innerPadding: PaddingValues`，Column 修饰符 `.padding(16.dp)` → `.padding(horizontal = 16.dp).padding(innerPadding)`（在 `.verticalScroll()` 之后）
- `AssistantMcpPage.kt` 需新增 import：`LargeFlexibleTopAppBar`、`TopAppBarDefaults`、`nestedScroll`、`LocalLayoutDirection`、`CustomColors`、`calculateStartPadding`、`calculateEndPadding`
- `SettingFilesPage.kt` 和 `SettingMcpPage.kt` 需新增 import：`calculateStartPadding`、`calculateEndPadding`、`LocalLayoutDirection`
- ⚠️ `SettingMcpPage.kt` 的 `loading` 计算逻辑有本地偏离（batch-3 MCP 生命周期），保留不修改
- ⚠️ `SettingFilesPage.kt` 同时被步骤 3（fileSizeToString）修改，两处变更需合并

### 步骤 10：扩展管理按钮关闭面板修复（1 个提交）

| # | 提交 | 文件 | 改动 |
|---|------|------|------|
| 20 | `5ea0e35f` | `FilesPicker.kt` | `InjectionQuickConfigSheet` 新增 `onDismissAll` 参数 + 3 个导航回调简化 + 删除 `scope`/`launch` import |

**具体改动**：
1. `InjectionQuickConfigSheet` 调用处（`:288`）新增 `onDismissAll = onDismiss`
2. `InjectionQuickConfigSheet` 签名（`:395`）新增 `onDismissAll: () -> Unit`
3. 删除 `val scope = rememberCoroutineScope()`（`:398`）
4. 3 个导航回调：
   ```kotlin
   // 旧：scope.launch { sheetState.hide(); onDismiss(); navController.navigate(...) }
   // 新：onDismissAll(); navController.navigate(...)
   ```
5. 删除 `import rememberCoroutineScope` + `import kotlinx.coroutines.launch`

### 步骤 11：工作区导入按钮移至顶栏（1 个提交）

| # | 提交 | 文件 | 改动 |
|---|------|------|------|
| 21 | `624ab635` | `WorkspaceDetailPage.kt` | 删除 `floatingActionButton` + 导入按钮移至 TopAppBar `actions` + 删除 `FloatingActionButton` import |

**说明**：FAB 遮挡文件菜单上下文菜单，移至顶栏 IconButton 解决。

### 步骤 12：验证

- 编译通过（`./gradlew assembleDebug`）
- 单元测试通过（`./gradlew test`）
- 逐文件与上游最终态核对
- lint 检查

---

## 五、本地偏离记录

### 本次同步涉及的本地偏离

| 文件 | 偏离 | 原因 |
|------|------|------|
| `mark.html` | 仅同步 mark.html，跳过 `web-ui/markdown.tsx` | Fork 未引入 Web UI 模块（同 batch-5） |
| `ModelRegistry.kt` | 无 `QWEN_MT`（上游有） | Fork 未引入 Qwen MT 模型 |
| `ChatCompletionsAPI.kt` | batch-4 同步的 Video/Audio 处理将被移除 | 上游 `89f8c4b1` 简化为仅 Text + Image |
| `GoogleProvider.kt` | 本地有 Imagen 实现（将被移除） | 上游 `1f937b8d` 移除 Google Imagen 支持 |
| `WorkspaceToolUIs.kt` | `formatBytes()` 为死代码（将被删除） | 上游确认 unused，本批统一清理 |
| `WebDavTab.kt` / `S3Tab.kt` / `S3Sync.kt` | 已使用 `fileSizeToString()`（本地文件，上游不涉及） | 本地备份功能使用，不受本批影响 |
| `Provider.kt` | `generateImage` 默认实现 + `ImageGenSize` 字段变更需合并 | #9 和 #15 均修改 Provider.kt，合并最终态 |
| `ImgGenPage.kt` | ImageGenSize 重构 + 移除重复模型选择器需合并 | #15 和 #16 均修改 ImgGenPage.kt，合并最终态 |
| `values-ja/ko-rKR/ru/strings.xml` | TTS 引号描述未更新（上游仅更新 en+zh） | 上游仅更新 2 个 locale，其他 locale 保持旧文案 |
| `SettingFilesPage.kt` | 步骤 3（fileSizeToString）与步骤 9（状态栏沉浸）均修改此文件 | 两处变更需合并：删除 `formatBytes()` + 改调用 + innerPadding 拆分 |
| `SettingMcpPage.kt` | `loading` 计算逻辑更复杂（含 Dormant/Authorizing） | batch-3 MCP 生命周期本地改进，保留不修改 |

---

## 六、持久化影响评估

| 改动 | 持久化影响 | 风险 |
|------|-----------|------|
| mark.html mhchem 修复 | 纯前端渲染，无持久化 | **无风险** |
| StringUtils.kt 弯引号修复 | 工具函数逻辑，无持久化 | **无风险** |
| StringUtilsTest.kt 新建 | 测试文件，无持久化 | **无风险** |
| fileSizeToString 重构 | 显示格式变化，无持久化 | **无风险** |
| ModelRegistry 新增模型 | 模型能力检测，无持久化 | **无风险** |
| ChatCompletionsAPI 工具结果修复 | API 请求构建逻辑，无持久化 | **无风险** |
| Provider/GoogleProvider Imagen 移除 | 代码删除，无持久化 | **无风险** |
| StringUtils.kt 直角引号扩展 | 工具函数逻辑，无持久化 | **无风险** |
| strings.xml TTS 引号描述更新 | 显示文案，无持久化 | **无风险** |
| ImageGenSize 重构 | `ImageAspectRatio` → `ImageGenSize` 枚举重命名 + `aspectRatio` → `size: String` 字段变更 | **低风险** — `ImageGenerationParams`/`ImageEditParams` 为内存对象非持久化，但 `@Serializable ImageAspectRatio` 枚举重命名可能影响序列化兼容性（本地无持久化场景） |
| ImgGenPage 移除重复模型选择器 | UI 代码删除，无持久化 | **无风险** |
| 状态栏沉浸修复（9 文件） | UI 布局调整，无持久化 | **无风险** |
| FilesPicker 面板关闭修复 | UI 交互逻辑，无持久化 | **无风险** |

> 本批同步绝大部分为代码级改动。imggen 重构涉及 `@Serializable` 枚举重命名（`ImageAspectRatio` → `ImageGenSize`），但该枚举仅用于内存中的 `ImageGenerationParams`/`ImageEditParams`，不直接持久化到 Room/DataStore，无序列化兼容风险。无 Room schema 变更、无 DataStore 配置结构变更、无 AndroidManifest 变更。

---

## 七、与前批同步的关联

本批同步中有多处是对前批同步引入功能的后续修复：

| 本批提交 | 关联前批 | 关系 |
|----------|----------|------|
| `4d2276d0` + `0df1939c` | batch-5 `b1a9f0ba`（mhchem 导入） | 修复 mhchem 导入方式和 \ce 渲染问题 |
| `89f8c4b1` | batch-4 `2232c65d`（OpenAI 多模态工具调用） | 修复工具结果图片无条件回传导致不支持图片的模型报错 |
| `8eeaa801` + `152e8614` + `e1ef629b` + `57e6dbc1` | — | 新增 bug 修复 + 功能增强 + 配套测试 |
| `0217ca82` + `c66935f2` | — | imggen 重构 + UI 清理 |
| `5f5f9654` + `5ea0e35f` | — | 状态栏沉浸修复 + 扩展面板关闭修复 |

---

*分析完成日期：2026-07-08（含补充检查 2 个新提交）*
*代码审查完成日期：2026-07-08*

---

## 八、代码审查记录

### 8.1 上游新增提交检查

同步代码应用后，多次 `git fetch upstream` 发现上游新增 3 个提交：

| 提交 | 描述 | 判定 | 原因 |
|------|------|------|------|
| `a2a71da2` | feat: 新增 MiniMax 和 MIMO 内置提供商 | ❌ 跳过 | 在 `DefaultProviders.kt` 新增 MiniMax（Claude API）和 MIMO（OpenAI API）两个内置 Provider，与 Fork 精简方向直接冲突（新 Provider）|
| `624ab635` | fix(ui): 工作区导入按钮移至顶栏避免遮挡文件菜单 | ✅ 同步 | UI 修复，FAB 遮挡文件菜单上下文菜单，移至顶栏 IconButton 解决 |
| `d7f0ef26` | feat: 支持 HEIF/HEIC 图片上传与裁剪 (#1286) | ✅ 同步 | 扩展 HEIF/HEIC/AVIF 品牌码识别（FileEncoder + FileUtils），裁剪前将 HEIF 转为 JPEG 规避 UCrop 解码失败（ImageUtils + ChatPage），功能增强不与 Fork 精简方向冲突 |

### 8.2 逐文件审查结果

全部 32 个变更文件逐一审查，与上游最终态核对一致：

| 文件 | 审查结果 | 备注 |
|------|----------|------|
| `mark.html` | ✅ 正确 | mhchem side-effect import + `{ katex }` 参数，与上游一致 |
| `StringUtils.kt` | ✅ 正确 | 弯引号 Unicode 转义 + 直角引号扩展 + fileSizeToString 重写，与上游一致 |
| `StringUtilsTest.kt` | ✅ 正确 | 11 个测试方法，包名映射正确，与上游一致 |
| `ModelRegistry.kt` | ✅ 正确 | 8 个新模型定义 + 列表条目，位置正确，无 `QWEN_MT`（本地偏离） |
| `ChatCompletionsAPI.kt` | ✅ 正确 | 6 处改动（参数链 + toToolResultContent 重写），与上游最终态逐行核对一致 |
| `ChatCompletionsAPIMessageTest.kt` | ✅ 正确 | 测试反射调用更新为 3 参数签名，与上游一致 |
| `Provider.kt` | ✅ 正确 | generateImage 默认 error() + ImageGenSize 字段变更，两处提交合并正确 |
| `GoogleProvider.kt` | ✅ 正确 | Imagen 实现（~82 行）+ 4 个 import 删除，与上游一致 |
| `OpenAIProvider.kt` | ✅ 正确 | 2 处 when 表达式 → if isNotBlank() 简化，与上游一致 |
| `Image.kt` | ✅ 正确 | ImageAspectRatio → ImageGenSize 枚举重命名，8 个 size 选项 |
| `ImgGenPage.kt` | ✅ 正确 | ImageGenSize UI 重构 + 重复模型选择器移除 + 硬编码字符串 i18n 修复（本地改进） |
| `ImgGenVM.kt` | ✅ 正确 | 状态/方法重命名 + 参数变更，与上游一致 |
| `McpPicker.kt` | ✅ 正确 | contentPadding 参数新增 |
| 6 个助手详情页 | ✅ 正确 | innerPadding 移入滚动容器，统一模式 |
| `AssistantMcpPage.kt` | ✅ 正确 | TopAppBar → LargeFlexibleTopAppBar + nestedScroll |
| `SettingFilesPage.kt` | ✅ 正确 | innerPadding 拆分 + formatBytes 删除 + fileSizeToString（两处变更合并） |
| `SettingMcpPage.kt` | ✅ 正确 | PullToRefreshBox padding 移除 + LazyColumn contentPadding（loading 偏离保留） |
| `FilesPicker.kt` | ✅ 正确 | onDismissAll 参数 + 3 个导航回调简化 + scope/launch import 删除 |
| `WorkspaceToolUIs.kt` | ✅ 正确 | 死代码 formatBytes() 删除 |
| `WorkspaceDetailPage.kt` | ✅ 正确 | formatBytes 删除 + fileSizeToString 调用 + import + FAB 移至顶栏（`624ab635`） |
| 5 个 `values*/strings.xml` | ✅ 正确 | TTS 引号描述更新 + 新增 3 个 imggen size 字符串 + 删除 7 个废弃字符串（本地改进） |
| `upstream-sync.md` | ✅ 正确 | 第六批摘要 + 跳过列表 + 同步规则更新 |
| `FileEncoder.kt` | ✅ 正确 | HEIF/HEIC/AVIF 品牌码扩展（`d7f0ef26`），ftyp box 检测 + 12 个品牌码 |
| `FileUtils.kt` | ✅ 正确 | HEIF/HEIC/AVIF 嗅探检测（`d7f0ef26`），与 FileEncoder 逻辑一致 |
| `ImageUtils.kt` | ✅ 正确 | isHeifImage + convertHeifToJpeg + HEIF_BRANDS（`d7f0ef26`），复用已有 calculateInSampleSize/correctImageOrientation/recycleBitmapSafely |
| `ChatPage.kt` | ✅ 正确 | HEIF→JPEG 转换前置 UCrop 裁剪（`d7f0ef26`），fallback 保持原有复制逻辑 |

### 8.3 发现的问题与处理

#### 问题 1：Gradle 版本升级（本地独立维护）

`gradle/wrapper/gradle-wrapper.properties` 从 Gradle 9.5.0 升级到 9.6.1，此变更不属于 batch-6 范围内的上游提交，为本地独立维护变更（用户安排）。

#### 问题 2：ImgGenPage 硬编码字符串 → ✅ 已修复

上游 `0217ca82` 遗留的 3 处硬编码字符串（"Image Size"、"Custom size"、"e.g. 1024x1024"）已进行 i18n 处理，新增 `imggen_page_image_size` / `imggen_page_custom_size` / `imggen_page_custom_size_placeholder` 三个字符串资源，在 5 个 locale 文件中定义。

#### 问题 3：废弃字符串资源 → ✅ 已清理

imggen 重构后不再被代码引用的 7 个字符串资源（`imggen_page_aspect_ratio*` 5 条 + `imggen_page_model_selection*` 2 条）已从 5 个 locale 文件中删除，共清理 35 条废弃字符串。

#### 问题 4：English strings.xml 引号转义不一致（继承自上游）

`values/strings.xml` 中 TTS 引号描述使用 `\'\'`（转义 ASCII 单引号），而 `values-zh/strings.xml` 使用 `'``'`（U+2018/U+2019 弯单引号）。经核实上游 `e1ef629b` 同样如此，此为上游不一致，本地继承。

#### 问题 5：文档步骤编号缺失 → ✅ 已修复

执行计划步骤编号从 7 跳到 9 的问题已修复，新增步骤 8（i18n 修复 + 字符串清理）和步骤 11（工作区导入按钮移至顶栏），原步骤 11（验证）改为步骤 12。

### 8.4 Linter 检查

对所有变更的 Kotlin 文件 + 测试文件 + strings.xml 执行 linter 检查，**无错误**。

### 8.5 编译与测试验证

- `gradlew.bat compileDebugKotlin`：**BUILD SUCCESSFUL**（18s，84 tasks）
- `gradlew.bat test`：**BUILD SUCCESSFUL**（14s，199 tasks），所有模块单元测试通过（ai/app/search 等）
- 编译过程无代码级警告（仅有 Gradle/Java 模块系统环境警告）

### 8.6 审查中修复的问题

| 问题 | 文件 | 修复 |
|------|------|------|
| `label` 参数缩进缺失 | `ImgGenPage.kt:742` | 恢复 24 空格缩进 |
| 双 BOM（`EF BB BF EF BB BF`） | `FileUtils.kt`、`ImageUtils.kt` | 移除多余 BOM，恢复原始编码 |
| strings.xml 新增行缩进缺失 | 5 个 `values*/strings.xml` | 恢复 2 空格缩进（6 行 × 5 文件 = 30 行） |
