# 上游同步分析 — 第八批（2026-07-11）

> 本文档为第八批上游同步检查的完整分析记录，包含逐提交 diff 分析、本地代码核对、适配要点。
> 进度摘要见 [`docs/dev/upstream-sync.md`](../upstream-sync.md)。
>
> **同步状态：✅ 全部完成（编译通过 + 单元测试通过 + 逐文件核对一致）**

---

## 一、背景

### 1.1 项目概况

- **项目**：Measix Pilot — 从 [RikkaHub](https://github.com/rikkahub/rikkahub) fork 的 Android LLM 聊天客户端
- **Fork 基线**：RikkaHub 2.3.1（versionCode 164）— 2026-06-18
- **本地当前版本**：0.0.9（versionCode 9）
- **本地 DB 版本**：version = 3（含文件夹表）
- **包名映射**：上游 `me.rerere.rikkahub` → 本地 `net.weero.measix.pilot`
- **DeepLink scheme**：上游 `rikkahub://` → 本地 `measix://`

### 1.2 上次同步状态

- **上次检查点**：`8e872e5f`（2026-07-08，第七批）
- **上次同步范围**：`624ab635..8e872e5f`（2026-07-08，10 个提交，5 个已同步）

### 1.3 本次检查范围

- **检查范围**：`8e872e5f..upstream/master`（2026-07-09 ~ 2026-07-11，共 **11 个提交**）
- **最新上游提交**：`1ff465d1`（2026-07-11，自定义 JS 搜索 scrape 参数补全）
- **上游版本**：2.4.1（versionCode 169，未升级）

### 1.4 版本号对应关系

| 维度 | 上游 | 本地 | 说明 |
|------|------|------|------|
| 版本名 | 2.4.1 | 0.0.9 | 版本线独立，不跟随上游版本号 |
| versionCode | 169 | 9 | 独立递增 |
| DB 版本 | v24 | v3 | 本地从 v1 重建，无历史 migration 包袱 |

> 本批同步不递增本地版本号（`versionCode`/`versionName` 保持 9/0.0.9），改动以 bug 修复和功能增强为主。

---

## 二、提交清单

### 2.1 需同步（8 个提交，按时间从旧到新）

| # | 提交 | 日期 | 描述 | 判定 | 优先级 | 分组 |
|---|------|------|------|------|--------|------|
| 1 | `b6b16842` | 07-09 | TTS 不朗读括号内的内容，与引号过滤叠加生效 (#1473) | ✅ 同步 | P1 | A |
| 2 | `449ce1e6` | 07-09 | 补充 removeBracketedContent 单元测试并补齐 TTS 括号设置本地化 | ✅ 同步 | P1 | A |
| 3 | `d910c4a1` | 07-10 | 预览内容改为缓存到文件避免导航状态过大崩溃 (#1485) | ✅ 同步 | P0 | B |
| 4 | `dab4e448` | 07-10 | 修正 TTS 本地工具描述，改为所选 TTS 服务 | ✅ 同步 | P1 | A |
| 5 | `40592c1b` | 07-10 | 恢复备份后目录缺失改为标记 BROKEN 而非删除记录 | ✅ 同步 | P0 | C |
| 6 | `d37d4ed6` | 07-10 | 优化建议回复默认提示词以帮助改进 AI 回答 (#1489) | ✅ 同步 | P2 | E |
| 7 | `0dfb598c` | 07-11 | 补全中英文 TTS 引号描述，优化英文引号显示 (#1502) | ✅ 同步 | P1 | A |
| 8 | `1ff465d1` | 07-11 | 为自定义 JS 搜索的 scrape 参数补全 array items 声明 (#1504) | ✅ 同步 | P0 | D |

> 分组 A 的 4 个提交（#1, #2, #4, #7）涉及 TTS 朗读功能，合并最终态一次引入。

### 2.2 跳过（3 个提交）

| 提交 | 日期 | 描述 | 跳过原因 |
|------|------|------|----------|
| `9d337938` | 07-10 | fix stale upstream unit tests (#1492) | **已同步**：`ChatCompletionsAPIMessageTest` 已有 3 参数签名（batch-6 同步）；`highlight/material3/search` 的 `build.gradle.kts` 已有 `testImplementation(libs.junit)`；MiMo 测试已是 `mimo-v2.5-tts`；ElevenLabs/FishAudio 测试不存在于 fork（已精简 TTS） |
| `3c1a2de6` | 07-10 | 捕获 OEM 拒绝 FGS 权限导致 Web 服务器启动闪退 (#1495) | **Fork 未引入 Web 模块**：本地无 `WebServerService.kt` 和 `WebServerManager.kt` |
| `cf55cbbb` | 07-10 | CI 仓库拥有者提交的 Issue 跳过空白模板自动关闭 | **CI 配置，不适用**：与 batch-7 跳过的 CI 配置一致 |

---

## 三、逐提交 diff 分析与本地代码核对

### 分组 A：TTS 朗读优化（4 个提交合并引入）

#### A1. `b6b16842` — TTS 不朗读括号内的内容 (#1473)

**上游变更**：

| 文件 | 变更 |
|------|------|
| `PreferencesStore.kt` | `DisplaySetting` 新增 `ttsOnlyReadOutsideBrackets: Boolean = false` |
| `StringUtils.kt` | 新增 `removeBracketedContent()` 扩展函数：正则 `\([^)]*?\)|（[^）]*?）` 移除中英文括号内容，全空返回 null |
| `ChatMessageActions.kt` | TTS 朗读逻辑从 if-else 改为链式过滤：先 `extractQuotedContentAsText`，再 `removeBracketedContent` |
| `TTSAutoPlay.kt` | 同上链式过滤 |
| `SettingPreferencesGeneralPage.kt` | 新增 `ttsOnlyReadOutsideBrackets` 开关项 |
| `strings.xml` (en) | 新增 `setting_display_page_tts_read_outside_brackets_title` / `_desc` |
| `strings.xml` (zh) | 同上中文翻译 |

**本地代码核对**：

- `PreferencesStore.kt` 第 524 行：已有 `ttsOnlyReadQuoted`，紧邻位置可插入新字段 ✅
- `StringUtils.kt` 第 142 行（文件末尾）：可追加 `removeBracketedContent` ✅
- `ChatMessageActions.kt` 第 137-141 行：当前为 if-else 逻辑，需改为链式 ✅
- `TTSAutoPlay.kt` 第 25-29 行：当前为 if-else 逻辑，需改为链式 ✅
- `SettingPreferencesGeneralPage.kt` 第 276-287 行：有 `ttsOnlyReadQuoted` 开关，紧邻位置可插入 ✅
- `strings.xml` (en/zh)：需新增 2 条字符串 ✅

**适配要点**：
- 包名映射：`me.rerere.rikkahub` → `net.weero.measix.pilot`
- `removeBracketedContent()` 正则中的中文括号 `（）` 需确保字符编码正确

#### A2. `449ce1e6` — 补充 removeBracketedContent 单元测试 + 本地化

**上游变更**：

| 文件 | 变更 |
|------|------|
| `StringUtilsTest.kt` | 新增 8 个测试用例：英文括号、中文括号、多括号、trim、不跨边界、全空返回 null、无括号返回原文 |
| `strings.xml` (ja/ko/ru/zh-rTW) | 新增 `tts_read_outside_brackets` 翻译 |

**本地代码核对**：
- `StringUtilsTest.kt` 第 66 行（文件末尾）：可追加 8 个测试 ✅
- 本地无 `values-zh-rTW` 目录，该 locale 翻译跳过 ✅
- ja/ko/ru 3 个 locale 需新增翻译 ✅

#### A3. `dab4e448` — 修正 TTS 本地工具描述

**上游变更**：

| 文件 | 变更 |
|------|------|
| `strings.xml` (en) | `assistant_page_local_tools_tts_desc`: "device TTS engine" → "your selected TTS service" |
| `strings.xml` (zh/ja/ko/ru/zh-rTW) | 同步翻译 |

**本地代码核对**：
- `strings.xml` (en) 第 86 行：当前为 "device TTS engine"，需改为新文案 ✅
- 全部 5 locale 均需更新 ✅
- 本地无 `values-zh-rTW`，跳过 ✅

#### A4. `0dfb598c` — 补全中英文 TTS 引号描述 (#1502)

**上游变更**：

| 文件 | 变更 |
|------|------|
| `strings.xml` (en) | `tts_only_read_quoted_desc`: 补全 `""` `''` 中文引号显示，引号间加空格 |
| `strings.xml` (zh) | 同上中文 |

**本地代码核对**：
- `strings.xml` (en) 第 670 行：当前为 `When enabled, TTS will only read content within quotation marks (including "", '', 「」, 『』)`，需改为新格式 ✅
- `strings.xml` (zh) 第 669 行：同上 ✅

**上游最终文案**：
- en: `When enabled, TTS will only read content within quotation marks (including\" \", \' \', "", '', 「」, 『』)`
- zh: `启用后，TTS 仅朗读引号（包括\" \"、\' \'、"",'、「」、『』）内的内容`

**适配要点**：
- 注意 XML 转义：`"` → `\"`，`'` → `\'`
- 本地已有 `""` `''` `「」` `『』`，上游补的是 `\" \"`（英文双引号加空格）和 `\' \'`（英文单引号加空格）

---

### 分组 B：WebView 预览内容缓存到文件（1 个提交）

#### B1. `d910c4a1` — 预览内容缓存到文件避免导航状态过大崩溃 (#1485)

**Bug 背景**：大段 HTML 通过导航参数（`Screen.WebView(content = ...)`）传递，撑爆 `SaveableStateRegistry`/`Bundle`，触发 `TransactionTooLargeException` 崩溃。

**上游变更**：

| 文件 | 变更 |
|------|------|
| `WebViewContentCache.kt` (新文件) | SHA-256 内容 ID + 文件缓存（`cacheDir/webview_content/`），7 天自动清理，按 SHA-256 去重 |
| `RouteActivity.kt` | `Screen.WebView` 的 `content: String` 改为 `contentId: String`；导航调用改为传 `contentId` |
| `ChatMessage.kt` | `base64Encode()` → `WebViewContentCache.store(context.cacheDir, htmlContent)` |
| `HighlightCodeBlock.kt` | 同上 + 新增 `val context = LocalContext.current` |
| `Mermaid.kt` | 同上 |
| `WebViewPage.kt` | `base64Decode()` → `WebViewContentCache.load(context.cacheDir, contentId)` |
| `WebViewContentCacheTest.kt` (新文件) | 3 个测试：大内容存储、相同内容去重、无效 ID 拒绝 |

**本地代码核对**：

- `RouteActivity.kt` 第 614 行：`data class WebView(val url: String = "", val content: String = "")` → 需改 `contentId` ✅
- `RouteActivity.kt` 第 397 行：`WebViewPage(key.url, key.content)` → 需改 `key.contentId` ✅
- `ChatMessage.kt` 第 233 行：`navController.navigate(Screen.WebView(content = htmlContent.base64Encode()))` → 需改 ✅
- `ChatMessage.kt` 第 94 行：`import ...base64Encode` → 需移除，新增 `import ...WebViewContentCache` ✅
- `HighlightCodeBlock.kt` 第 464 行：同上需改 ✅
- `HighlightCodeBlock.kt` 第 83 行：`import ...base64Encode` → 需移除 ✅
- `HighlightCodeBlock.kt`：`HighlightCodeActions` 函数内需新增 `val context = LocalContext.current` ✅
- `Mermaid.kt` 第 129 行：同上需改 ✅
- `Mermaid.kt` 第 36 行：`import ...base64Encode` → 需移除 ✅
- `WebViewPage.kt` 第 46 行：`fun WebViewPage(url: String, content: String)` → 需改 `contentId` ✅
- `WebViewPage.kt` 第 58 行：`content.base64Decode()` → `WebViewContentCache.load(context.cacheDir, contentId).orEmpty()` ✅
- `WebViewPage.kt` 第 42 行：`import ...base64Decode` → 需移除 ✅
- 本地 `baseUrl` 已是 `measix.local`（上游是 `rikkahub.local`），无需改 ✅
- `ChatMessage.kt` 中 `context` 变量在作用域内已有（`onClick` lambda 内可通过 `context` 访问），需确认上下文 ✅

**适配要点**：
- 新文件 `WebViewContentCache.kt` 包名改为 `net.weero.measix.pilot.ui.components.webview`
- `WebViewContentCache` 是 `internal object`，需保持可见性
- 移除不再使用的 `base64Encode`/`base64Decode` import（注意检查其他文件是否仍有使用）
- `HighlightCodeBlock.kt` 中 `HighlightCodeActions` 需新增 `LocalContext` import 和获取

---

### 分组 C：Workspace 恢复备份后目录缺失处理（1 个提交）

#### C1. `40592c1b` — 目录缺失改为标记 BROKEN 而非删除记录

**Bug 背景**：工作区记录存于数据库（会被备份），但工作区文件不进备份。恢复旧数据库后，`checkIntegrity` 因目录缺失误删工作区记录及助手绑定，导致用户工作区配置丢失。

**上游变更**：

| 文件 | 变更 |
|------|------|
| `WorkspaceRepository.kt` | `checkIntegrity()` 中目录缺失时：从 `dao.deleteById()` + `cleanupAssistantReferences()` 改为 `updateShellState(id, BROKEN.name)`（仅当当前状态非 BROKEN 时更新） |
| `RikkaHubApp.kt` | 注释更新：`remove orphaned DB records` → `mark workspaces with missing files as broken` |

**本地代码核对**：

- `WorkspaceRepository.kt` 第 37-41 行：当前逻辑为删除 + 清理引用，需改为标记 BROKEN ✅
- `MeasixPilotApp.kt` 第 77 行：注释需更新 ✅
- 本地 `WorkspaceShellStatus.BROKEN` 枚举值已存在（`WorkspaceRepository.kt` 第 134 行已使用）✅
- `updateShellState(workspaceId, shellStatus)` 方法已存在（第 272-280 行）✅

**上游最终态**（第 37-42 行）：
```kotlin
if (!dir.exists()) {
    Log.w(TAG, "Workspace directory missing, marking as broken: id=${workspace.id}, root=${workspace.root}")
    if (workspace.shellStatus != WorkspaceShellStatus.BROKEN.name) {
        updateShellState(workspace.id, WorkspaceShellStatus.BROKEN.name)
    }
    continue
}
```

---

### 分组 D：CustomJsSearchService scrape 参数修复（1 个提交）

#### D1. `1ff465d1` — 为 scrape 参数补全 array items 声明 (#1504)

**Bug 背景**：`urls` 数组缺少 `items` 字段导致 Gemini/Claude API 校验失败返回 400。

**上游变更**：

| 文件 | 变更 |
|------|------|
| `CustomJsSearchService.kt` | `scrapingParameters()` 中 `urls` 数组补全 `items: { type: string }` |

**本地代码核对**：
- 第 41-44 行：当前 `urls` 只有 `type: array` + `description`，缺少 `items` ✅

**上游最终态**（第 41-47 行）：
```kotlin
put("urls", buildJsonObject {
    put("type", "array")
    put("description", "urls to scrape")
    put("items", buildJsonObject {
        put("type", "string")
    })
})
```

---

### 分组 E：建议回复提示词优化（1 个提交）

#### E1. `d37d4ed6` — 优化建议回复默认提示词 (#1489)

**上游变更**：

| 文件 | 变更 |
|------|------|
| `Suggestion.kt` | `DEFAULT_SUGGESTION_PROMPT` 第 5 行："responses to the assistant" → "responses to help the assistant improve its answers" |

**本地代码核对**：
- `Suggestion.kt` 第 5 行：当前为旧文案，需改为新文案 ✅

**上游最终态**：
```
You need to act as the **User** to reply to the assistant, generating 3~5 appropriate and contextually relevant responses to help the assistant improve its answers.
```

---

## 四、持久化与迁移影响评估

| 变更 | 类型 | 影响 | 风险 |
|------|------|------|------|
| `DisplaySetting.ttsOnlyReadOutsideBrackets` | `@Serializable` data class 新增字段 | 有默认值 `false`，旧 `settings.json` 正常反序列化 | 无 |
| `Screen.WebView` 参数名变更 | `@Serializable` data class 字段改名 | 运行时导航参数，不持久化 | 无 |
| `WorkspaceRepository.checkIntegrity()` 行为变更 | 行为改变 | 目录缺失时标记 BROKEN 而非删除记录 | 无，仅改善行为 |
| `CustomJsSearchService` schema 补全 | 工具参数 schema 补全 | 仅修复 API 兼容性 | 无 |
| `Suggestion.kt` 提示词修改 | 文本变更 | 仅影响新会话的建议生成 | 无 |
| `strings.xml` 文案修正 | i18n 文本变更 | 不影响功能 | 无 |

**结论**：本批同步无 Room schema 变更、无 DataStore 结构变更、无新依赖、无 minSdk 变更，持久化迁移风险为零。

---

## 五、同步操作计划

### 5.1 引入顺序

建议按分组依次引入，每个分组完成后编译验证：

1. **分组 D**（搜索参数修复）— 1 个文件，3 行改动，最简单
2. **分组 E**（提示词优化）— 1 个文件，1 行改动
3. **分组 C**（Workspace 修复）— 2 个文件
4. **分组 A**（TTS 朗读优化）— 4 个提交合并，7 个源文件 + 5 个 strings.xml + 1 个测试文件
5. **分组 B**（WebView 缓存）— 1 个新文件 + 5 个修改文件 + 1 个新测试文件

### 5.2 分组 A 详细文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `PreferencesStore.kt` | 修改 | 新增 `ttsOnlyReadOutsideBrackets` 字段 |
| `StringUtils.kt` | 修改 | 新增 `removeBracketedContent()` 函数 |
| `ChatMessageActions.kt` | 修改 | TTS 朗读逻辑改为链式过滤 |
| `TTSAutoPlay.kt` | 修改 | 同上 |
| `SettingPreferencesGeneralPage.kt` | 修改 | 新增开关项 |
| `StringUtilsTest.kt` | 修改 | 新增 8 个测试用例 |
| `strings.xml` (values) | 修改 | 新增 2 条 + 修改 2 条 |
| `strings.xml` (values-zh) | 修改 | 新增 2 条 + 修改 2 条 |
| `strings.xml` (values-ja) | 修改 | 新增 2 条 + 修改 1 条 |
| `strings.xml` (values-ko-rKR) | 修改 | 新增 2 条 + 修改 1 条 |
| `strings.xml` (values-ru) | 修改 | 新增 2 条 + 修改 1 条 |

### 5.3 分组 B 详细文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `WebViewContentCache.kt` | 新增 | SHA-256 内容缓存 |
| `WebViewContentCacheTest.kt` | 新增 | 3 个测试用例 |
| `RouteActivity.kt` | 修改 | `content` → `contentId` |
| `ChatMessage.kt` | 修改 | `base64Encode` → `WebViewContentCache.store` |
| `HighlightCodeBlock.kt` | 修改 | 同上 + 新增 `LocalContext` |
| `Mermaid.kt` | 修改 | 同上 |
| `WebViewPage.kt` | 修改 | `base64Decode` → `WebViewContentCache.load` |

### 5.4 验证检查点

- [x] 分组 D 编译通过
- [x] 分组 E 编译通过
- [x] 分组 C 编译通过
- [x] 分组 A 编译通过 + 单元测试通过
- [x] 分组 B 编译通过 + 单元测试通过
- [x] 全量 `assembleRelease` 编译通过
- [x] 逐文件与上游最终态核对一致
- [x] `changelog.md` 更新
- [x] `upstream-sync.md` 状态更新为 ✅

---

## 六、本地偏离记录

本批同步无预计的本地偏离。所有改动均可直接映射到本地代码，仅需注意：
- 包名映射：`me.rerere.rikkahub` → `net.weero.measix.pilot`
- DeepLink：`rikkahub.local` → `measix.local`（本地已是正确值）
- 本地无 `values-zh-rTW` 目录，该 locale 的翻译跳过

---

*分析完成日期：2026-07-11*
*同步完成日期：2026-07-11*
