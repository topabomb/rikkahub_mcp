# 上游同步分析 — 第五批（2026-07-06）

> 本文档为第五批上游同步检查的完整分析记录，包含逐提交 diff 分析、本地代码核对、适配要点。
> 进度摘要见 [`docs/dev/upstream-sync.md`](../upstream-sync.md)。
>
> **同步状态：✅ 全部完成（2026-07-06，编译通过 + 逐文件核对一致）**
>
> 6 个提交一次执行完毕：代码修复（3）+ 功能增强（2）+ 依赖升级（1，含预存偏离对齐）。

---

## 一、背景

### 1.1 项目概况

- **项目**：Measix Pilot — 从 [RikkaHub](https://github.com/rikkahub/rikkahub) fork 的 Android LLM 聊天客户端
- **Fork 基线**：RikkaHub 2.3.1（versionCode 164）— 2026-06-18
- **本地当前版本**：0.0.9（versionCode 9）
- **本地 DB 版本**：version = 3（含文件夹表）
- **包名映射**：上游 `me.rerere.rikkahub` → 本地 `net.weero.measix.pilot`
- **DeepLink scheme**：上游 `rikkahub://` → 本地 `measix://`

### 1.2 上次同步状态

- **上次检查点**：`5b39e05d`（2026-07-03，第四批）
- **上次同步范围**：`4b2fd4b9..5b39e05d`（2026-07-01 ~ 2026-07-03，9 个提交，全部同步）
- **永久跳过**：赞助商 / 推荐提供商 / Serper 搜索 / StepFun TTS / ElevenLabs TTS

### 1.3 本次检查范围

- **检查范围**：`5b39e05d..ef564dca`（2026-07-05，共 **8 个提交**）
- **最新上游提交**：`ef564dca`（2026-07-05，发布 2.4.0 版本）
- **上游版本**：2.4.0（versionCode 168）

### 1.3.1 版本号对应关系

| 维度 | 上游 | 本地 | 说明 |
|------|------|------|------|
| 版本名 | 2.4.0 | 0.0.9 | 版本线独立，不跟随上游版本号 |
| versionCode | 168 | 9 | 独立递增 |
| 发布提交 | `ef564dca` | — | 上游版本升级提交，本地跳过 |
| DB 版本 | v24 | v3 | 本地从 v1 重建，无历史 migration 包袱 |
| MCP SDK | 0.14.0 | 0.14.0 | batch-4 已对齐 |

> 本批同步未递增本地版本号（`versionCode`/`versionName` 保持 9/0.0.9），因 batch-4 已递增至 0.0.9 且本批改动均为补丁级修复和依赖升级。

### 1.4 核对方法

按工作方法执行：`git fetch upstream` → `git log --oneline 5b39e05d..upstream/master` → 逐提交 `git show <hash> --stat` + `git show <hash> -- <path>` 获取完整 diff → 打开本地对应文件逐行核对。

---

## 二、提交逐项分析

### 2.1 `b1a9f0ba` — feat(markdown): webui 和 WebView 新增化学公式渲染支持 (#1440)

| 项目 | 内容 |
|------|------|
| 描述 | 为 KaTeX 添加 mhchem 扩展，支持 `\ce{}` 等化学公式语法；同时为 katex.render 添加 `trust: true` 选项 |
| 影响文件 | `app/src/main/assets/html/mark.html`、`web-ui/app/components/markdown/markdown.tsx` |

**上游 diff（mark.html）**：

1. 新增 mhchem 扩展导入与注册：
```javascript
import katexMhchem from 'https://esm.sh/katex@0.16.8/dist/contrib/mhchem.mjs';
katex.use(katexMhchem);
```

2. inline 和 block 两处 `katex.render` 均新增 `trust: true`：
```javascript
katex.render(el.textContent, el, {
    throwOnError: false,
    errorColor: '#cc0000',
    trust: true   // ← 新增
});
```

**上游 diff（markdown.tsx）**：
```tsx
import "katex/dist/contrib/mhchem.mjs";
// ...
rehypePlugins={[[rehypeKatex, { trust: true, strict: false }], rehypeRaw]}
```

**本地核对**：
- `mark.html:180` — 仅有 `import katex`，**无 mhchem 导入** ❌
- `mark.html:269-272` — inline 渲染**无 `trust: true`** ❌
- `mark.html:280-284` — block 渲染**无 `trust: true`** ❌
- `web-ui/` 目录 — **本地不存在**（上游 Web UI，Fork 未引入），该文件跳过

**判定**：✅ **同步** — 功能增强

**适配**：仅同步 mark.html（3 处改动），跳过 web-ui/markdown.tsx

---

### 2.2 `195287fa` — fix(chat): 修复预览搜索跳转被键盘滚动打断 (#1432)

| 项目 | 内容 |
|------|------|
| 描述 | 预览模式下点击搜索结果跳转到消息时，`animateScrollToItem` 会被键盘滚动事件打断导致跳转失败。改用 `requestScrollToItem` 即时定位 |
| 影响文件 | `ui/pages/chat/ChatPage.kt` |

**上游 diff**：
```kotlin
// ChatPage.kt, onJumpToMessage 回调
- chatListState.animateScrollToItem(index)
+ chatListState.requestScrollToItem(index)
```

**本地核对**：
- `ChatPage.kt:454` — 仍使用 `animateScrollToItem(index)` ❌
- `requestScrollToItem` 在本地已广泛使用（`ChatPage.kt:187,353,367`、`ChatList.kt:278`、`ModelList.kt:633,636`），是本地已有的扩展函数

**判定**：✅ **同步** — bug 修复，与本地已有模式一致

**适配**：无（单行替换，`requestScrollToItem` 已在本地使用）

---

### 2.3 `57651267` — fix(ui): 修复搜索选择面板在残留内置搜索状态下显示空白

| 项目 | 内容 |
|------|------|
| 描述 | 不支持内置搜索的模型若残留 `BuiltInTools.Search` 状态，SearchPicker 两张卡片都被跳过导致面板空白且无入口关闭。改为已开启内置搜索时也显示开关 |
| 影响文件 | `ui/components/ai/SearchPicker.kt` |

**上游 diff**：
```kotlin
- // 模型内置搜索
- if (model != null && (ModelRegistry.GEMINI_SERIES.match(model.modelId) || model.modelId.contains("gpt-"))) {
+ // 模型是否支持内置搜索
+ val supportsBuiltInSearch = model != null &&
+     (ModelRegistry.GEMINI_SERIES.match(model.modelId) || model.modelId.contains("gpt-"))
+ // 模型是否已开启内置搜索（可能是不支持的模型残留的孤儿状态）
+ val hasBuiltInSearchEnabled = model?.tools?.contains(BuiltInTools.Search) == true
+
+ // 模型支持内置搜索，或已开启内置搜索（后者保证残留状态也能被关闭）时显示开关
+ if (model != null && (supportsBuiltInSearch || hasBuiltInSearchEnabled)) {
      BuiltInSearchSetting(model = model)
  }

  // 如果没有开启内置搜索，显示搜索服务选择
- if (model?.tools?.contains(BuiltInTools.Search) != true) {
+ if (!hasBuiltInSearchEnabled) {
```

**本地核对**：`SearchPicker.kt:162-167` 与上游修复前代码**完全一致** ❌

**判定**：✅ **同步** — bug 修复

**适配**：无（直接替换，`ModelRegistry`/`BuiltInTools` 在本地一致）

---

### 2.4 `fb1da5d5` — feat: latex 支持 `\ce`

| 项目 | 内容 |
|------|------|
| 描述 | 升级 jlatexmath 1.4 → 1.5，使原生 LaTeX 渲染器支持 `\ce{}` 化学公式语法（与 #1 mark.html mhchem 配套） |
| 影响文件 | `gradle/libs.versions.toml` |

**上游 diff**：
```toml
- jlatexmath = "1.4"
+ jlatexmath = "1.5"
```

**本地核对**：`libs.versions.toml:5` — `jlatexmath = "1.4"` ❌

**判定**：✅ **同步** — 功能依赖升级，与 #1 配套

**适配**：无（minor 版本升级，jlatexmath 引用通过 `version.ref` 自动生效）

---

### 2.5 `c0c7f70e` — chore(deps): 升级 Compose BOM、Material3 和 Navigation3 版本

| 项目 | 内容 |
|------|------|
| 描述 | Compose BOM 2026.06.00→2026.06.01，Material3 1.5.0-alpha22→1.5.0-alpha23，Navigation3 1.1.3→1.1.4 |
| 影响文件 | `gradle/libs.versions.toml` |

**上游 diff**：
```toml
- composeBom = "2026.06.00"
+ composeBom = "2026.06.01"
- material3 = "1.5.0-alpha22"
+ material3 = "1.5.0-alpha23"
- nav3Core = "1.1.3"
+ nav3Core = "1.1.4"
```

**本地核对与全链路版本对比**：

| 依赖 | 本地 | 上游修复前 | 上游修复后 | 差距 |
|------|------|-----------|-----------|------|
| `composeBom` | `2026.06.00` | `2026.06.00` | `2026.06.01` | +1 补丁版本 |
| `material3` | `1.5.0-alpha22` | `1.5.0-alpha22` | `1.5.0-alpha23` | +1 alpha 版本 |
| `nav3Core` | `1.1.2` | `1.1.3` | `1.1.4` | **跨 2 版本**（本地比上游修复前还旧 1 版本） |

**预存偏离发现**（非本提交引入，但属于同一 AndroidX/Nav3 生态）：

| 依赖 | 本地 | 上游 | 偏离 |
|------|------|------|------|
| `material3AdaptiveNav3` | `1.3.0-beta02` | `1.3.0-rc01` | beta→rc，跨 1 版本 |
| `lifecycleViewmodelNav3` | `2.10.0` | `2.11.0` | +1 minor |
| `lifecycleRuntimeKtx` | `2.10.0` | `2.11.0` | +1 minor |
| `nav2` | 不存在 | `2.9.8` | Fork 移除，不引入 |

**评估结论**：

1. `composeBom` 2026.06.00→2026.06.01：BOM 补丁版本，管理传递依赖版本，低风险 ✅
2. `material3` alpha22→alpha23：一个 alpha 迭代，通常为 API 补充/修复，低风险 ✅
3. `nav3Core` 1.1.2→1.1.4：跨 2 个 minor 版本。1.1.x 应向后兼容，但需编译验证 ✅
4. `material3AdaptiveNav3` beta02→rc01：adaptive-navigation3 与 nav3Core 配套工作。RC 版本已有 API 稳定化。nav3Core 1.1.4 在上游与 rc01 一起测试过，beta02 可能不兼容 ✅ **对齐**
5. `lifecycleViewmodelNav3` 2.10.0→2.11.0：viewmodel-navigation3 与 nav3Core 配套。2.11.0 可能依赖 lifecycle 2.11.0 ✅ **对齐**
6. `lifecycleRuntimeKtx` 2.10.0→2.11.0：lifecycle 核心库，lifecycleViewmodelNav3 2.11.0 依赖它 ✅ **对齐**

**判定**：✅ **同步** — 依赖升级 + 预存偏离对齐

**适配**：同步 3 个直接变更 + 3 个预存偏离对齐，共 6 个版本号修改，编译验证

---

### 2.6 `b3f298f1` — docs: 将赞助商名称从随想AI网关改为随想AI中转

| 项目 | 内容 |
|------|------|
| 描述 | README 中赞助商名称文字替换 |
| 影响文件 | `README.md`、`README_ZH_CN.md` |

**判定**：❌ **跳过** — 赞助商体系已移除

---

### 2.7 `0edcd81b` — refactor(ui): 压缩上下文对话框保留消息数改为手动输入

| 项目 | 内容 |
|------|------|
| 描述 | 用 `OutlinedNumberInput` 替换 `SingleChoiceSegmentedButtonRow`，支持自由输入保留的最近消息数量（原来只能选 0/16/32/64） |
| 影响文件 | `ui/components/ai/CompressContextDialog.kt` |

**上游 diff**：
```kotlin
// 删除
val keepRecentOptions = listOf(0, 16, 32, 64)

// 替换 SegmentedButtonRow 为
OutlinedNumberInput(
    value = keepRecentMessages,
    onValueChange = { keepRecentMessages = it },
    label = stringResource(R.string.chat_page_compress_keep_recent),
    modifier = Modifier.fillMaxWidth(),
)
```

**本地核对**：
- `CompressContextDialog.kt:42` — 仍有 `val keepRecentOptions = listOf(0, 16, 32, 64)` ❌
- `CompressContextDialog.kt:111-125` — 仍使用 `SingleChoiceSegmentedButtonRow` ❌
- ✅ `OutlinedNumberInput` 已在本地存在：`ui/components/ui/Input.kt:19`，签名匹配

**判定**：✅ **同步** — UI 改进

**适配**：
- 删除 `keepRecentOptions` 变量
- 替换 `SingleChoiceSegmentedButtonRow` 块为 `OutlinedNumberInput` 调用
- 新增 import：`OutlinedNumberInput`
- 保留 `SingleChoiceSegmentedButtonRow`/`SegmentedButton`/`SegmentedButtonDefaults` import（tokenOptions 选择器仍在使用）

---

### 2.8 `ef564dca` — chore: 发布 2.4.0 版本 (versionCode 168)

| 项目 | 内容 |
|------|------|
| 描述 | 版本号升级 2.3.4 → 2.4.0（versionCode 167 → 168） |
| 影响文件 | `app/build.gradle.kts` |

**判定**：❌ **跳过** — 版本线独立

---

## 三、汇总判定

| # | 提交 | 描述 | 判定 | 优先级 |
|---|------|------|------|--------|
| 1 | `b1a9f0ba` | 化学公式渲染支持（mark.html mhchem + trust） | ✅ 同步 | P1 |
| 2 | `195287fa` | 预览搜索跳转 `requestScrollToItem` | ✅ 同步 | P0 |
| 3 | `57651267` | 搜索选择面板残留状态修复 | ✅ 同步 | P0 |
| 4 | `fb1da5d5` | jlatexmath 1.4→1.5（`\ce` 支持） | ✅ 同步 | P1（与 #1 配套） |
| 5 | `c0c7f70e` | Compose BOM/Material3/Nav3 升级 + 预存偏离对齐 | ✅ 同步 | P1 |
| 6 | `b3f298f1` | 赞助商名称文档 | ❌ 跳过 | — |
| 7 | `0edcd81b` | 压缩上下文对话框手动输入 | ✅ 同步 | P1 |
| 8 | `ef564dca` | 版本号升级 2.4.0 | ❌ 跳过 | — |

**跳过**：2 个提交（赞助商文档 + 版本号）
**需同步**：6 个提交（#1~#5 + #7），其中 #5 包含 3 个预存偏离对齐

---

## 四、同步执行计划

全部 6 个提交一次执行，按依赖关系排序：

### 步骤 1：代码修复（3 个提交）

| # | 提交 | 文件 | 改动 |
|---|------|------|------|
| 2 | `195287fa` | `ChatPage.kt:454` | `animateScrollToItem` → `requestScrollToItem` |
| 3 | `57651267` | `SearchPicker.kt:162-167` | 提取变量 + 条件逻辑修改 |
| 7 | `0edcd81b` | `CompressContextDialog.kt` | SegmentedButton → OutlinedNumberInput |

### 步骤 2：功能增强（2 个提交）

| # | 提交 | 文件 | 改动 |
|---|------|------|------|
| 1 | `b1a9f0ba` | `mark.html` | +mhchem 导入/使用 + `trust: true` ×2 |
| 4 | `fb1da5d5` | `libs.versions.toml:5` | `jlatexmath = "1.5"` |

### 步骤 3：依赖升级（1 个提交 + 预存偏离对齐）

| # | 提交 | 文件 | 改动 |
|---|------|------|------|
| 5 | `c0c7f70e` | `libs.versions.toml` | 6 个版本号修改 |

### 步骤 4：验证

- 编译通过（`./gradlew assembleDebug`）
- 逐文件与上游最终态核对
- lint 检查

---

## 五、本地偏离记录

### 本次同步涉及的本地偏离

| 文件 | 偏离 | 原因 |
|------|------|------|
| `mark.html` | 仅同步 mark.html，跳过 `web-ui/markdown.tsx` | Fork 未引入 Web UI 模块 |
| `libs.versions.toml` | 无 `nav2` 版本（上游有 `nav2 = "2.9.8"`） | Fork 移除 Navigation2 依赖 |
| `libs.versions.toml` | `material3AdaptiveNav3` / `lifecycleViewmodelNav3` / `lifecycleRuntimeKtx` 预存偏离 | 历史批次未对齐，本次一并对齐 |

### 预存偏离对齐说明

以下 3 个依赖版本在本次同步前已落后于上游（非 `c0c7f70e` 引入），本次一并对齐以降低 nav3 生态兼容性风险：

| 依赖 | 对齐前 | 对齐后 | 对齐原因 |
|------|--------|--------|----------|
| `material3AdaptiveNav3` | `1.3.0-beta02` | `1.3.0-rc01` | adaptive-navigation3 与 nav3Core 1.1.4 配套，RC 版本经上游测试 |
| `lifecycleViewmodelNav3` | `2.10.0` | `2.11.0` | viewmodel-navigation3 与 nav3Core 1.1.4 配套 |
| `lifecycleRuntimeKtx` | `2.10.0` | `2.11.0` | lifecycleViewmodelNav3 2.11.0 依赖 lifecycle 2.11.0 |

---

## 六、持久化影响评估

| 改动 | 持久化影响 | 风险 |
|------|-----------|------|
| mark.html mhchem + trust | 纯前端渲染，无持久化 | **无风险** |
| ChatPage.kt requestScrollToItem | UI 滚动行为，无持久化 | **无风险** |
| SearchPicker.kt 条件修复 | UI 显示逻辑，无持久化 | **无风险** |
| CompressContextDialog OutlinedNumberInput | UI 输入方式，无持久化 | **无风险** |
| jlatexmath 1.4→1.5 | 渲染库升级，无持久化 | **无风险** |
| composeBom/material3/nav3 升级 | 编译时依赖，无持久化 | **需编译验证** |
| material3AdaptiveNav3/lifecycle 对齐 | 编译时依赖，无持久化 | **需编译验证** |

---

*分析完成日期：2026-07-06*

---

## 七、同步完成确认

### 执行结果

| 提交 | 文件 | 状态 | 说明 |
|------|------|------|------|
| `195287fa` | `ChatPage.kt` | ✅ 完成 | `animateScrollToItem` → `requestScrollToItem` |
| `57651267` | `SearchPicker.kt` | ✅ 完成 | 提取变量 + 残留状态条件修复 |
| `0edcd81b` | `CompressContextDialog.kt` | ✅ 完成 | SegmentedButton → OutlinedNumberInput |
| `b1a9f0ba` | `mark.html` | ✅ 完成 | mhchem 导入 + `trust: true` ×2（跳过 web-ui） |
| `fb1da5d5` | `libs.versions.toml` | ✅ 完成 | jlatexmath 1.4→1.5 |
| `c0c7f70e` | `libs.versions.toml` | ✅ 完成 | 3 直接变更 + 3 预存偏离对齐 |

### 验证

- 编译通过（`./gradlew assembleDebug`）
- 逐文件与上游最终态核对一致（仅包名差异 + web-ui 跳过）
- lint 检查通过

*同步完成日期：2026-07-06*
